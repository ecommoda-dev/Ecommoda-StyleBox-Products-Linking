<div dir="rtl" style="text-align: right;">

# Ecommoda StyleBox Products Linking (`StyleBox-Products-Linking`)

![version](https://img.shields.io/badge/version-v2.2.0-blue)

> ⚠️ **أداة جديدة بالكامل من الصفر (مسار N — §9 في ecommoda-tool-migration-playbook)،
> مش نقل أداة موجودة من الداشبورد.** الملف `index.js` كان تاريخيًا
> `shopify-woo-sync-worker` (tool = `shopify_woo_sync`) — اتعمل عليه رينيم كامل
> + إعادة بناء شاملة مقابل `ecommoda-worker-builder` و`woocommerce-sync-helper`
> يوم 25-08-2026 (راجع تعليق §CONSTANTS في أول `index.js` لتفاصيل كل تغيير).
> الكود اتسلّم جاهزًا بالكامل من جلسة Cowork سابقة ونُقل هنا بايت ببايت من غير
> أي لمس لمنطقه.

**بتعمل إيه:** لكل منتج WooCommerce متغيّر مربوط بمنتج Shopify (الربط عبر
`global_unique_id`)، بتزامن الـ SKU/المخزون/الـ metafields لكل Variation، بتحدّث
حالة وعنوان وتاج المنتج على شوبيفاي، وبتنشر المنتج على ووردبريس (`status=publish`).
**مين بيستخدمها:** فريق ربط المنتجات (WooCommerce ↔ Shopify).
**الإصدار:** Worker `v2.12.0` · الواجهة `v2.17.0`.

## مطابقة المهارات بعد مراجعة شاملة — Worker v2.12.0 · واجهة v2.16.0 (10-09-2026)

> **المصدر:** جرد `skills-sweep` كامل + مراجعة يدوية لكل بند مالوش وصفة فحص.
> **السبب الجذري:** البصمة كانت متجمّدة عند `worker-builder v1.0.0` و
> `html-builder v1.0.0` بينما المهارتين بقوا **v3.0.0** و**v7.0.0** — 68 بند
> فاتوا، منهم 17 🔴. الأداة اتبنت **صح** على قواعد قديمة، وورثت بندين كانوا
> **كود غلط جوّه المهارة نفسها** (الإزاحة الثابتة · `parseInt` بلا حراسة).
>
> ⚠️ **كل البنود دي كانت بتشتغل في صمت** — مفيش خطأ ولا توقّف. عشان كده
> كانت محتاجة جرد عشان تتلقى أصلاً.

### 1) 🔴 التوقيت بقى يتحسب بـ `Intl`، مش إزاحة ثابتة

- `toCairo()` كانت `getTime() + 3*60*60*1000`. التوقيت الصيفي بيخلص
  **29-10-2026** — من اليوم ده كل وقت معروض وكل صف تصدير بيغلط بساعة، والثابت
  **مابيشتكيش**. البديل مجموعة `Intl` بـ`timeZone:'Africa/Cairo'`
  (`cairoParts`/`cairoOffsetMinutes`/`cairoDateStr`).
- ⚠️ **نفس الدوال بالحرف في `index.js` §HELPERS::time وفي `index.html`
  §SHELL-JS** — نسختين مختلفتين = الشاشة والسجل بيقولوا وقتين مختلفين لنفس الصف.
- **والإزاحة تتقاس عند ظهر اليوم مش منتصف الليل** (`cairoDayBoundsUTC`) —
  التحويل بيحصل فجرًا، فقياس منتصف الليل بيقع في الساعة المكرّرة/المفقودة.
- ✅ **الأداة دي مش محتاجة أي تدخل يوم 29-10-2026.**

### 2) 🔴 فلتر التاريخ في تاب السجل كان بيقارن UTC بتواريخ قاهرة

- `r.date.substring(0,10)` = تاريخ **UTC**، والعمود المعروض بتوقيت القاهرة —
  تقويمان مختلفان في نفس الشاشة. كل عملية بين **١٢ و٣ الفجر** كانت بتتحسب على
  اليوم اللي قبله، في الفلترة **وفي التصدير**. بقى `cairoDateStr(...)` في
  الاتنين.

### 3) 🔴 عقد `get_logs_export` — الصفوف **والحقيقة** مع بعض

- `getLogsExport` بتقص عند `LOG_EXPORT_MAX = 2000` **في السكوت**. والحالة هنا
  **أوسع من البند الأصلي**: الأداة بتحمّل تاب السجل كله من الـ endpoint ده،
  فالسقف بيقص **الشاشة** و**عدّاد «النتائج»** كمان مش الملف بس.
- الـ Worker بقى يرجّع `{ok, entries, cap, total, truncated}` — و`getLogsCount`
  بيتنادى **بالتوازي بنفس الفلاتر بالظبط** (نداء بفلاتر مختلفة بيطلّع نسبة
  كذّابة، وهي أسوأ من مفيش رقم).
- الواجهة: بانر ثابت `#logExportWarn` فوق الجدول بالرقمين + توست، ورسالة
  `تم تصدير N ✓` بقت في فرع `else` **بس**. **وممنوع الواجهة تكتب `2000`** —
  السقف بيرجع من الـ Worker كـ`cap`.

### 4) 🔴 §SHARED — كتلة السجل اتحدّثت للنسخة الحالية

- `buildLogFilterSQL()` **بنّاء واحد** للتلات دوال. قبله كان SQL مكرر و`type`
  بيتفلتر في `getLogs` **بس** — يعني التصدير والعدّ بينزّلوا غير المعروض، ومن
  غير أي رسالة. وبقى بيقبل `employees[]`/`types[]` و`dateFrom`/`dateTo`.
- `LOG_SORT_COLUMNS` **قايمة بيضاء مقفولة** + `orderByClause()` بـ**كاسر تعادل
  إلزامي** (القيمة جاية من العميل وبتتلزق في نص SQL — `ORDER BY` مابيقبلش bind).
- `logParamsFrom(url, TOOL_NAME)` — مصدر واحد لفلاتر التلات endpoints.
- `parseInt` على `limit`/`offset` بقى محروس بـ`Number.isFinite` —
  `parseInt('abc')` → `NaN` → `Math.min(NaN,100)` → `NaN` بيوصل لـD1 كـbind.

### 5) 🔴 حالات النتيجة بقت أربعة — `rejected` مش `error`

- مفردات `extra.result` الرسمية (`ecommoda-constants` §12) اتكتبت على **كل** صف
  لوج. الفرق اللي البند اتكتب عشانه:
  - **`error`** = حاولنا، النداء وصل للنظام الخارجي واترفض → أحمر.
  - **`rejected`** = اتوقف **قبل** أي محاولة → محايد.
- التلاتة اللي كانوا أحمر غلط: **حارس البراند** · **التعارض في
  `find_product_relink`** · **«مفيش منتج بالرقم ده»**. تلاتتهم مالمسوش ولا
  منصة. في الواجهة بقى ليهم بادج `⊘ مرفوض — مفيش أي تعديل` وعدّاد مستقل في
  بانر Bulk، و**زرار «أعد محاولة الفاشلة» بيستثنيهم** (إعادة المحاولة من غير
  ما البراند يتضاف هترجع بنفس النتيجة بالظبط).
- ومقاس **اتكتب فعلاً على ووكومرس** وفشل ميتافيلد شوبيفاي بعده بقى
  `type:'synced'` + `extra.result:'warning'` بدل `type:'error'` — تسجيله فشل
  كان بيعمل **حفرة مقفولة**: الصف بيقول فشل والمقاس متزامن (Step 5A ⑩②).
- **استعلام خط الأساس الصحيح بقى:**
  ```sql
  … AND tool='stylebox_products_linking' AND type='synced'
    AND json_extract(extra,'$.result') = 'success'
  ```
  الاستعلام اللي بيعدّ `type` لوحده **بيقيس المحاولات مش الكتابة**.

### 6) 🔴 توست: الأنواع الأربعة معرّفة + الأحمر للفشل الحقيقي بس

- `.toast-warning` **ماكانتش معرّفة خالص** في CSS الأداة (success/error/neutral
  بس) — أول نداء بالنوع الرسمي كان هيدّي **توست بلا خلفية** (نص أبيض على شفاف).
- وكان فيه **25 نداء بـ`'error'` وصفر بـ`'warning'`**. اتحوّل **21** منهم
  لتحذيرات (حرّاس ومدخلات: «سجّل دخولك أولاً» · «فيه تشغيلة شغّالة» · «فرق
  السعر لازم يكون رقم»)، و«تم ربطه من قبل» بقى `neutral`. الأحمر لكل حاجة
  بيعلّم الموظف يتجاهل الأحمر كله.

### 7) 🔴 حارس نسخة الـ Worker بقى حد أدنى بمقارنة رقمية

- `EXPECTED_WORKER_VERSION` + `!==` اتشالوا لصالح **`MIN_WORKER_VERSION`
  (`v2.12.0`) + `cmpVersion()`**. التطابق الحرفي كان بيولّع على أي Worker
  **أحدث** كمان، والحالة الحقيقية (rollback · Promote ناقص) بتضيع وسط إنذار كاذب.
- الأسماء الموحّدة: `verStaleBtn` / `showWorkerStale()` (القديمة
  `verMismatchBtn`/`showVersionMismatch()` **مهجورة**).
- ⚠️ **`MIN_WORKER_VERSION` بترتفع مع أي حقل جديد الواجهة تبقى بتقراه من الـ
  Worker** — دلوقتي `v2.12.0` بسبب `cap`/`total`/`truncated`. لو نسيت ترفعها،
  الحارس نفسه بيبقى **معطّل** ويعدّي Worker مش قادر يخدم الصفحة.

### 8) 🟡 شاشة الإعدادات: خانة واحدة بس — الـ Secret (واجهة v2.17.0)

- `WORKER URL` و`ADMIN WORKER URL` **اتشالوا نهائيًا** من شاشة الإعدادات
  (`html-builder v2.2.0` · Standards #28). الاتنين **مش سر**: الحماية الحقيقية
  في `WORKER_SECRET` + الـ CORS allowlist، مش في إخفاء الرابط — واسم الـ Worker
  ثابت من وقت الإنشاء ومايتغيّرش. فتحويلهم لحقل إدخال كان **عبء على الموظف بلا
  أي مكسب أمني**.
- `WORKER_URL` بقى **ثابت في `§CONFIG`** — لو حصل rename أو custom domain، ده
  المكان الوحيد اللي يتغيّر.
- ⚠️ **`ADMIN WORKER URL` ماكانش مستخدم في الأداة دي من الأساس** — كان بيتخزّن
  ويتحمّل ويتحفظ ومفيش ولا نداء واحد بيقراه. كل نداءات الـ Auth
  (`get_employees`/`verify_employee`/…) بتروح للـ Worker بتاع الأداة نفسها.
  فاتشال بالكامل بدل ما يتحوّل لثابت ميت.
- `LS_URL` و`LS_ADMIN_URL` اتشالوا — **`LS_SECRET` بقى المفتاح الوحيد** في
  `localStorage`. و`isConfigured()` بقت تفحص السر بس.
- ⚠️ **الأداة دي مش عضو في أي مجموعة سر** (`ecommoda-constants` §6)، فالمفتاح
  فضل باسمها: `stylebox_link_worker_secret`.
- ⚠️ **المفتاحين القدام في متصفحات الموظفين اتسابوا زي ما هم** — مابقاش حد
  بيقراهم، ومسحهم مالوش فايدة. الموظف مش محتاج يعمل أي حاجة: الرابط اللي كان
  محفوظ عنده بقى معروف للأداة من نفسها.

### 9) 🟡 الباقي

- **حارس `WORKER_SECRET` الغايب قبل مقارنة الـ auth** — من غيره القالب بينتج
  السلسلة الحرفية `"Bearer undefined"`، فأي طلب معاه الهيدر ده **بيعدّي** على
  Worker بيغيّر حالة منتجات ومخزون.
- **مهلة `apiRequest` = 90 ثانية** (`AbortController`). ⚠️ نص الرسالة جزء من
  البند: بيقول إن العملية **ممكن تكون تمّت** وإن مكان التحقق **تاب السجل**.
- **`checkWorkerVersion()` بقت تبدأ بـ`if (!isConfigured()) return;`** — من
  غيره مودال الإعدادات كان بيفتح **لوحده** على أي جهاز جديد فوق شاشة الدخول.
- **`esc()` على كل قيمة من D1 في `renderLogTable`** — عمود `notes` بيحمل نص
  خطأ ووكومرس حرفيًا، وعلامة `<` واحدة بتبلع باقي الصف.
- **عمودا التاريخ/الوقت انفصلوا في الجدول** (زي التصدير بالظبط)، والاتنين
  `sortable-th` مستقل **بنفس الـ timestamp الكامل** كمصدر ترتيب.
- **اسم ملف التصدير بتوقيت القاهرة** — كان `toISOString()`، فتصدير الساعة ١
  صباحًا بياخد تاريخ إمبارح.
- **نداء ووكومرس في `diag` بقى يعدّي من `wcFetch`** بـ`maxAttempts:1` — كان
  النداء الوحيد المتبقي بـ`fetch` مباشر. المحاولة الواحدة مقصودة: التشخيص
  بيعرض الحالة **الخام**، وإعادة المحاولة بتخفي خنق قايم.

### اللي **ما اتغيّرش** عن قصد

- **منطق الربط نفسه (`syncProduct`) متغيّرش ولا سطر** — ترتيب التنفيذ، حارس
  البراند، تصحيح الـ slug، دفعة المقاسات، عزل المقاس، تاج `stylebox` آخر خطوة.
- **كل حرّاس المسار الفردي** (زرار المراجعة · نافذة التأكيد · حارس «اتربط قبل
  كده» · الأسئلة التلاتة بدون افتراضي) زي ما هم بالظبط.
- **الأداة لسه manual-only** — مفيش `sync_all` ومفيش Cron.
- **تاب السجل لسه client-side** على تحميل واحد كامل (مفيش صفحات server-side).
  ⚠️ **لو اتحوّل لصفحات من السيرفر، `applySort` لازم تبقى تمرير مباشر
  (`return rows`) والترتيب يروح للـ Worker في نفس التسليم** — وإلا السهم في
  الترويسة هيرتّب 100 صف ويوعد بترتيب عام (`data-table-standard` §8).

## تقليل نداءات ووكومرس (10 → 5 لكل منتج) — Worker v2.11.0 · واجهة v2.15.0 (10-09-2026)

> **ليه:** بعد إصلاح v2.10.0، التشغيلة لسه بتتحجب — وتحليل الأرقام أدّى لنتيجتين:
>
> **① الـ 429 مش من ووكومرس أصلاً — رده فاضي.** WordPress/WooCommerce **دايمًا**
> بترجّع JSON فيه `code`/`message` (زي `product_invalid_sku` بالظبط). رد 429
> فاضي = الحجب حصل **قبل** ما الطلب يوصل لـ PHP — يعني **طبقة الاستضافة/WAF**
> (LiteSpeed/Hostinger — نفس الطبقة اللي `bust()` موجودة عشانها من الأصل).
> **مفيش إعداد في ووكومرس هيغيّر الحد ده.**
>
> **② الحد ~30 نداء، ونافذته أطول من 15 ثانية.** التشغيلة الأصلية اتحجبت عند
> المنتج الرابع (~30 نداء)، ومحاولة الإعادة اتحجبت عند الرابع برضه (~31 نداء)،
> والوقفة الثابتة 15 ثانية + 3 محاولات بباكوف **ما فكّتش الحجب**.
>
> **الخلاصة: الإيقاع لوحده مش كفاية — لازم عدد النداءات نفسه يقل.**

### 1) ⭐ كل المقاسات في نداء واحد — `/products/{id}/variations/batch`

- كان **نداء PUT لكل مقاس** (5-6 من أصل ~10 نداءات لكل منتج). دلوقتي نداء واحد
  لكل المنتج مهما كان عدد المقاسات (`wcBatchUpdateVariations()`، السقف 50 لكل
  دفعة تحت سقف ووكومرس 100 — راجع `woocommerce-sync-helper` Step 4).
- `syncProduct` بقت تلات مراحل: **(أ)** تجهيز محلي بدون أي نداء (مطابقة المقاس +
  حساب السعر + بناء الـ payload) · **(ب)** نداء الـ batch الواحد · **(ج)** لكل
  مقاس: نتيجته من الرد + ميتافيلد شوبيفاي + اللوج.
- ⚠️ **الرد بيرجع 200 حتى لو مقاس فشل** — الفشل بييجي جوه عنصر المقاس نفسه
  (`{id, error:{code,message,data}}`). فعزل المقاسات (v2.10.0) بيفضل **زي ما هو
  بالظبط**: مقاس فاشل بياخد تحذير بنص الخطأ وباقي المقاسات بتتزامن.
- فشل نداء الـ batch نفسه (زي 429 بعد كل المحاولات) = كل المقاسات تاخد نفس
  التحذير، والمنتج بيكمّل لخطوة التاج زي أي فشل معزول.

### 2) كاش البراند على مستوى الـ isolate

- نفس الـ Vendor كان بيتسأل عنه مع **كل منتج** (نداء WC كامل). `brandCache`
  (TTL 10 دقايق) بيشيل النداء ده من المنتج التاني وطالع.
- ⚠️ **الإيجابي بس هو اللي بيتخزّن** — لو البراند مش موجود، الحارس بيوقف الربط
  والموظف بيضيفه، فلازم المحاولة اللي بعدها تسأل من جديد وتلاقيه.

### 3) وقفة تصاعدية بتحترم `Retry-After`

- `wcFetch` بقى بيحط `Retry-After` في **نص الخطأ** (`(Retry-After: 45s)`)،
  والواجهة بتقراه منه (`retryAfterMsFromError()`) وتستنى المدة اللي الاستضافة
  طالباها فعلاً + ثانيتين هامش.
- من غير الهيدر: الوقفة بقت **تصاعدية** — 15s → 30s → 60s → 90s مع كل خنق
  متتالي (`bulkThrottleStreak`)، وبترجع لأولها مع أول منتج بينجح. الوقفة
  الثابتة (15s) كانت أقصر من نافذة الحظر فما كانتش بتفكّه.

### النتيجة

| | قبل | بعد |
|---|---|---|
| نداءات ووكومرس لكل منتج | **10-11** | **6** (وّ**5** بعد أول منتج بفضل كاش البراند) |
| منتجات قبل الحجب (~30 نداء) | ~3 | **~6** |

⚠️ **ده تخفيف مش حل نهائي.** الحل الجذري إن الاستضافة ترفع الحد أو تعمل
allowlist لـ Cloudflare Workers — راجع "مسائل مفتوحة".

## ⚠️ خنق ووكومرس (429) + عزل المقاسات — Worker v2.10.0 · واجهة v2.14.0 (10-09-2026)

> **عطل حقيقي في أول تشغيلة Bulk (نفس اليوم).** القائمة كانت 15 منتج: اتنين
> اتربطوا، التالت فشل بـ SKU مكرر، والرابع رجّع
> `WC update product 20758 failed: 429` — **ومن بعده كل نداء ووكومرس اترفض**،
> والواجهة عرضت كل ده كـ**"مفيش منتج على ووردبريس بالرقم ده"**.
> السبب الجذري: نداءات ووكومرس كانت **من غير أي retry ولا باكوف** (الـ retry
> كان لشوبيفاي بس من v2.0.0)، والمسح الاحتياطي كان بيرمي 30 نداء زيادة بعد كل
> فشل فيغذّي الخنق، والفشل كله كان بيتبلع في رسالة واحدة بتشاور على البيانات.

### 1) `wcFetch()` — الباب الوحيد لكل نداءات ووكومرس

- كل الـ helpers (`wcGetProduct`/`wcSearchProducts`/`wcSearchBrands`/
  `wcUpdateProduct`/`wcGetVariations`/`wcUpdateVariation`) بقت تعدّي منه.
- إعادة المحاولة على **429 · 5xx · فشل الشبكة** لحد `WC_MAX_ATTEMPTS = 3`،
  مع احترام هيدر `Retry-After` وإلا باكوف تربيعي (800ms → 3200ms، سقف 8s).
- الأخطاء بقت `WcHttpError` شايلة `status` — **الفرق بين 404 و429 هو أساس
  الإصلاح كله**.
- ⚠️ نصوص رسائل الخطأ **متغيّرتش** عن النسخ القديمة عمدًا (الواجهة والسجل
  بيعرضوها).

### 2) الخطأ الحقيقي بيوصل للموظف بدل "مفيش منتج"

- `findWcProductForRelink()`: أي خطأ WC **مش 404** بيترفع بنصّه (429/401/5xx)
  بدل ما يتبلع في fallback صامت. 404 بس = المنتج اتمسح فعلاً → بحث عادي.
- الواجهة بتفرّق: خنق → `⏱️ ووكومرس رفضت النداء ... المنتج ده ماتلمسش` بنص
  الخطأ، ومنتج مش مربوط → رسالة بتوجّهه لتاب الربط الفردي.

### 3) المسح الاحتياطي اتقفل في مسار Bulk بس

- `findWcProductByShopifyId(env, id, { skipScan })` — الخيار ده بيتبعت من
  `find_product_relink` **بس**. الرد بيرجع `scanSkipped:true` عشان الواجهة
  ماتقولش "مافحصناش كل المنتجات" وكأنه عطل.
- **تاب الربط الفردي لسه بيمسح زي ما هو بالظبط** — متغيّرش.

### 4) ⚠️ عزل كل مقاس في `try/catch` — باج قديم في المسار الفردي كمان

- قبل كده أي فشل من ووكومرس على مقاس (زي `product_invalid_sku` — SKU مكرر)
  كان **بيطلع بره `syncProduct` كله**: باقي المقاسات ما بتتزامنش، **وتاج
  `stylebox` (آخر خطوة) ما بيتضافش أصلاً**، والكتابات اللي حصلت قبله (حالة
  شوبيفاي + النجمة + الميتافيلد + `publish`) بتفضل متنفّذة → المنتج واقف في نصّه.
- دلوقتي المقاس الفاشل بياخد `status:'warning'` بنص الخطأ وباقي المنتج بيكمّل —
  نفس مبدأ عزل الخطوات 2/3/5 الموجود من الأول.
- **كل المقاسات فشلت = `overallStatus: 'error'`** مش `'warning'`.

### 5) إيقاع الواجهة في Bulk

- `BULK_PAUSE_MS = 700` بين كل منتج والتاني، و`BULK_THROTTLE_PAUSE_MS = 15000`
  بعد أي فشل شكله خنق (`isThrottleError()` بيقرا 429/502/503/504 من نص الخطأ).
- الهدف: القائمة تخلص كلها بدل ما تتقفل بعد تلات منتجات.

## مسار إعادة الربط الجماعي (Bulk) — Worker v2.9.0 · واجهة v2.13.0 (10-09-2026)

بطلب صريح من صاحب الأداة. **مسار إضافي بالكامل — المسار الفردي الموجود
(تاب "🔗 ربط منتج") متغيّرش ولا سطر فيه**، لا في الواجهة ولا في `sync_product`
ولا في `find_product`.

### 1) تاب تالت جديد "🧩 إعادة ربط Bulk" (`#panelBulk`)

- **الإدخال:** `textarea` واحد (`#bulkIdsInput`) — رقم منتج شوبيفاي في كل سطر،
  أو رابط صفحة المنتج (نفس `extractShopifyProductId()` بتاع المسار الفردي).
  `parseBulkIds()` بتشيل المكرّر وترصد السطور غير الصالحة، والعدّادات بتتحدّث
  لايف تحت المربع.
- **الخيارات:** نفس الأسئلة التلاتة بالظبط (الحالة / النجمة / تأكيد فرق السعر)
  بنفس القواعد الإلزامية بتاعة v2.11.0 — بس بتتحدد **مرة واحدة** وبتتطبّق على
  القائمة كلها. راجع `readBulkSyncOptions()`/`validateBulkStart()`.
- **التشغيل:** نافذة تأكيد إلزامية قبل أول نداء (`#bulkConfirmOverlay`) بتعرض
  عدد المنتجات والخيارات المختارة — فعل كتابة جماعي غير قابل للتراجع. بعدين
  شريط تقدّم + جدول بصف لكل منتج بحالته (✓ تمت / ⚠ تحذير / ✗ فشل / ⏹ اتوقف)،
  **قفل كامل للإدخال أثناء التشغيل** + حارس `beforeunload`، زرار "⏹ إيقاف"
  بيقف بعد المنتج الحالي، وزرار "🔁 أعد محاولة الفاشلة/المتوقفة" بعد الانتهاء.
- **منتج واحد لكل نداء `sync_product`** — الواجهة بتلفّ على القائمة بالترتيب
  (نداءين لكل منتج: `find_product_relink` ثم `sync_product`). **مفيش `sync_all`
  ومفيش Cron — الأداة لسه manual-only بالكامل**، ولازم موظف مسجّل دخول.
- فشل أي منتج **معزول تمامًا** عن باقي القائمة (زي عزل خطوات `syncProduct` في
  الـ Worker) — `brand_missing` بيتعرض في صف المنتج بزرار إضافة البراند بدل
  نافذة بتوقف كل حاجة.
- راجع `§BULK-JS` في `index.html`.

### 2) ⚠️ أكشن Worker جديد `find_product_relink` (v2.9.0) — إعادة الربط مسموحة هنا

```
GET ?action=find_product_relink&shopify_product_id=9714090869058
```

- قراءة بس، مفيش كتابة ومفيش D1 log — **بالظبط زي `find_product`**.
- **الفرق الوحيد:** المنتج المربوط قبل كده **مش سبب رفض هنا — ده الوضع المتوقع**.
  حارس "اتربط قبل كده" (`checkShopifyAlreadyLinked()`) لسه بيتنفّذ، لكن نتيجته
  بتُستخدم **كمصدر لرقم ووردبريس** بدل ما توقف العملية: قيمة `custom.wordpress_id`
  هي الربط نفسه، فمفيش داعي للبحث في ووكومرس أصلاً (نداء `wcGetProduct` واحد بدل
  ما يوصل لعشرات في المسح الاحتياطي — **3 subrequests في المسار الغالب**).
- **حارس التعارض:** رقم ووردبريس من الميتافيلد **مش كفاية لوحده** — الـ Worker
  بيتأكد إن منتج ووكومرس ده فعلاً بيخص رقم شوبيفاي المطلوب (GTIN حرفي أو بداية
  SKU). لو بيقول رقم شوبيفاي **تاني**، بيرجّع `{found:false, conflict:true}` من
  غير أي كتابة — ربط متقاطع غلط أسوأ بكتير من "مش لقيته". (GTIN فاضي وSKU من
  غير بادئة رقمية = **مش** تعارض — `syncProduct` بيتعامل معاها بالـ fallback المعتاد.)
- منتج لسه مش مربوط (أو ميتافيلد بيشاور على منتج مش موجود) → بيرجع للبحث العادي
  `findWcProductByShopifyId()` بالظبط زي المسار الفردي.
- راجع `findWcProductForRelink()` في `§BULK` في `index.js`.

### 3) الخطوات اللي **اتلغت في المسار الجماعي بس**

- زرار "🔗 راجع صور + كاتيجوريز المنتج قبل الربط" — **مش موجود هنا خالص**.
- نافذة "✏️ تأكيد المراجعة قبل الربط" (البندين checkbox) — **مش موجودة هنا خالص**.
- ⚠️ الاتنين **لسه إلزاميين زي ما هم في المسار الفردي** (v2.9.0/v2.10.0) — الإلغاء
  ده خاص بتاب Bulk بس، وطلب صريح من صاحب الأداة.

## تحصين البحث ضد فروق العنوان + تعديلات خطوة ④ — Worker v2.8.0 · واجهة v2.12.0 (27-08-2026)

### 1) ⚠️ البحث في `find_product` بقى محصّن ضد اختلاف العنوان بين المنصتين

**السبب:** حالة حقيقية (27-08-2026) — منتج موجود فعلاً على ووردبريس والـ SKU
بتاعه بيبدأ حرفيًا بالرقم المطلوب
(`10468826677570-skechers-go-run-trail-altitude-2-0-...`)، ومع ذلك البحث رجّع
**"اتفحص 0 منتج مرشّح"**. السبب كان **مسافة واحدة زيادة** في العنوان على
شوبيفاي (`Comfort  – in Dark brown`) مقابل مسافة واحدة على ووردبريس — البحث
بالعنوان كامل رجّع صفر مرشّحين، وبما إن فلتر الـ GTIN بيفشل أصلاً لأي منتج
لسه مش مربوط، مفيش مرشّح وصل أصلاً لمرحلة التأكيد بالـ SKU.

التعديل تلات مراحل بدل اتنين (`findWcProductByShopifyId()`):

1. **فلتر `global_unique_id`** — زي ما هو بالظبط، متغيّرش.
2. **بحث بالعنوان — بقى منضّف ومقصّر** (`buildTitleSearchQuery()`): أي تتابع
   مسافات بيتحوّل لمسافة واحدة، وبيتبعت **أول `TITLE_SEARCH_WORDS` (=4) كلمات
   بس** بدل العنوان كامل. تقصير الاستعلام بيوسّع شبكة المرشّحين، وبيحصّن البحث
   كمان ضد أي اختلاف في الكلمات المتأخرة. `per_page` اترفع من 25 لـ 50.
3. **(جديد) مسح احتياطي بالـ SKU** (`wcScanProductsBySkuPrefix()`) — الملاذ
   الأخير، **بيشتغل بس لو 1 و2 رجّعوا صفر مرشّحين**. بيمشي على منتجات ووكومرس
   صفحة صفحة (`per_page=100`، `orderby=date&order=desc` عشان المنتجات الجديدة —
   الحالة الغالبة — تتلاقى في أول صفحة أو صفحتين، `_fields` عشان الرد يبقى
   خفيف) وبيطابق محليًا على بداية الـ SKU. بيقف فورًا أول ما يلاقي.
   **مستقل تمامًا عن العنوان** — بيغطي أي اختلاف مهما كان (حرف، كلمة، عنوان
   مختلف بالكامل).

- **التأكيد النهائي متغيّرش خالص:** GTIN حرفي أو SKU بيبدأ بالرقم، أبدًا مش
  بالعنوان. اتنقل لدالة مشتركة `wcProductMatchesShopifyId()` عشان القاعدة
  تفضل في مكان واحد بين التأكيد على المرشّحين والمسح.
- **حقل جديد في الرد `scannedAll`** — `false` معناها المسح وقف عند سقف الصفحات
  (`SKU_SCAN_MAX_PAGES = 30` = 3000 منتج) أو فشل، يعني **"مش لقيته" مش قاطعة**.
  الواجهة بتقول للموظف كده صراحةً وبتفكّره بالحل اليدوي (حط الرقم في خانة GTIN).
  لو المتجر عدّى 3000 منتج، زوّد السقف ده.
- الحقل `matchedVia` (`search`/`scan`) بيقول التطابق جه من أنهي مرحلة.
- ⚠️ سقف الـ 30 صفحة = 30 subrequest كحد أقصى، وبس في الحالة النادرة اللي
  المرحلتين الأوليين بيفشلوا فيها. لو حد رفع السقف، ياخد باله من حد الـ
  subrequests بتاع Cloudflare Workers.

### 2) خطوة ④ "النتيجة" — تعديلان UI بحتان (بطلب صاحب الأداة)

- **جدول "فرق السعر المستخدم"** بقى بخط أكبر وBold (كلاس جديد
  `.price-diff-table`: القيمة 16px/800 بدل 13px/400 الموروثة من
  `.variant-table`) — كان مش واضح.
- **سطر ملخّص بانر "تم الربط بنجاح"** بقى **بنفس صيغة عنوان جدول المقاسات
  بالظبط**: `المقاسات: ✓ تمت (4) | ⚠ تحذير (0) | تخطي (0)` بدل
  `4 مقاس — ✓ 4 تمت المزامنة · ⚠ 0 تحذير · تخطي 0`.

## إلغاء الاختيار الافتراضي في أسئلة خطوة ③ + إعادة تصميم سؤال فرق السعر — واجهة v2.11.0 (27-08-2026)

بطلب صريح من صاحب الأداة — تعديل UI بحت، صفر تغيير في `index.js` أو أي
`action` أو أي payload (`price_difference` لسه بنفس المعنى والقيود القديمة —
راجع "خيارات `sync_product`" تحت، القيمة الوحيدة اللي اتغيّرت هي القيمة اللي
الحقل بيبدأ بيها في الواجهة).

- **الأسئلة التلاتة في خطوة ③** (حالة المنتج على شوبيفاي / النجمة / تحديث
  السعر) بقت **من غير أي اختيار افتراضي جاهز**: راديو "حالة المنتج" كان
  Draft محدد تلقائيًا، وراديو "النجمة" كان "نعم" محدد تلقائيًا — الاتنين
  دلوقتي **من غير أي زرار محدد** لحد ما الموظف يضغط بنفسه. `resetSyncOptions()`
  (بتتنادى مع كل منتج جديد/تسجيل خروج) بقت تشيل أي `checked` بدل ما تحط
  Draft/نعم.
- **حارس مزدوج في `runSync()`**: لو الموظف حاول يضغط "▶ ابدأ ربط المنتج بين
  الموقعين الآن" من غير ما يختار حالة شوبيفاي، أو من غير ما يختار النجمة، أو
  من غير ما يضغط زرار "تأكيد فرق السعر" (تحت)، العملية بترفض تمامًا وبيظهر
  تنبيه (`showToast`) — نفس نمط حارس `productReviewed` الموجود من v2.9.0.
  `readSyncOptions()` بترجع `null` (بدل قيمة افتراضية زي `'DRAFT'`) لما محدش
  مختار عشان الحارس يقدر يفرّق بين "مختار فعلاً" و"لسه من غير اختيار".
- **سؤال فرق السعر اتغيّر بالكامل** — العنوان بقى "💰 تحديث السعر على
  ووكومرس؟" (اتشالت "(اختياري)")، والمربع رقمي (`#priceDifference`) بقى بيبدأ
  دايمًا بقيمة **افتراضية 100** (بدل فاضي)، وجنبه **زرار جديد إلزامي "تأكيد
  فرق السعر"**:
  - نص الزرار بيتحدّث لايف مع أي رقم في المربع — مثلاً `100` → "تأكيد فرق
    السعر (+100) جنيه"، `50` → "تأكيد فرق السعر (+50) جنيه". المربع فاضي →
    "تأكيد تخطي تحديث السعر" (السلوك القديم — فاضي = تخطّي تحديث السعر
    تمامًا — **لسه زي ما هو تمامًا**، بس دلوقتي لازم تأكيده بالزرار برضه).
  - الضغط على الزرار هو "الاختيار" المطلوب لسؤال السعر (تمامًا زي ما اختيار
    راديو هو "الاختيار" للحالة/النجمة) — بيسجّل `priceDiffConfirmed = true`
    ويلوّن الزرار أخضر (`.confirmed`).
  - **أي تعديل في الرقم بعد الضغط بيلغي التأكيد فورًا** (`onPriceDiffInput()`
    بترجّع `priceDiffConfirmed = false` وتشيل `.confirmed`) — لازم يتضغط
    تاني قبل ما يقدر يكمل الربط.
  - القيمة "الأخيرة المستخدمة" في `localStorage` (`LS_PRICE_DIFF`) لسه بتتحفظ
    وبتتحمّل عند فتح الأداة (`loadPriceDiffPref()`) — لو مفيش قيمة محفوظة،
    الافتراضي بقى `100` بدل ما يفضل فاضي.
  - راجع `updatePriceConfirmLabel()`/`onPriceDiffInput()`/`confirmPriceDiff()`
    في `index.html`.
- ⚠️ القيود القديمة على `price_difference` **متغيّرتش**: لسه اختياري تمامًا
  من ناحية الـ payload (مفيش تحديث سعر لو الحقل فاضي عند التأكيد)، ولسه بيتبعت
  لـ `sync_product` بنفس الاسم، ومفيش أكشن منفصل ليه. التغيير هنا واجهة بحت:
  إزاي الموظف "بيأكد" اختياره قبل ما يقدر يضغط زرار الربط.

## زرار "إبدأ ربط منتج جديد" — لون ثابت بدل أبيض/أزرق عند hover — واجهة v2.10.1 (27-08-2026)

بطلب صريح من صاحب الأداة — تعديل UI بحت، صفر تغيير في `index.js` أو أي
`action` أو أي payload.

- زرار "🆕 بدء منتج جديد" (آخر خطوة ④) كان بيستخدم كلاس مخصص `.btn-new-product`
  بتصميم منفصل: خلفية بيضاء + إطار أزرق، بيتحوّل لخلفية زرقاء كاملة **بس عند
  مرور الماوس** (لونين مختلفين حسب حالة الزرار). بطلب المستخدم، الزرار بقى
  بلون ثابت زي باقي أزرار الأداة الزرقاء (نفس تصميم زرار "بحث"/"راجع صور +
  كاتيجوريز المنتج قبل الربط") — بيستخدم كلاس `.btn-primary.btn-block` القياسي
  دلوقتي، وبيغمق شوية بس عند hover (نفس سلوك كل الأزرار الزرقاء التانية، مش
  تغيير لون كامل).
- النص اتغيّر من "🆕 بدء منتج جديد" لـ**"إبدأ ربط منتج جديد"** (بدون إيموجي).
- كلاس `.btn-new-product` القديم اتشال بالكامل من CSS `index.html` (كان
  مستخدم في مكان واحد بس).
- راجع السطر اللي فيه `startNewProduct()` في `index.html`.

## نافذة تأكيد المراجعة الإلزامية + إعادة ترتيب "تعديلات تلقائي" + صياغة نتيجة المقاسات — واجهة v2.10.0 (27-08-2026)

بطلب صريح من صاحب الأداة — ثلاث تعديلات UI بحتة، **صفر تغيير في `index.js` أو أي
`action` أو أي payload**.

### 1) نافذة تأكيد إلزامية بعد زرار "راجع صور + كاتيجوريز المنتج قبل الربط"

- الزرار في خطوة ② (نتيجة البحث) لسه بيفتح صفحة المنتج على ووردبريس في تاب
  جديد زي ما هو، لكن دلوقتي كمان بيفتح **نافذة تأكيد إلزامية**
  (`#reviewConfirmOverlay`) فيها بندان checkbox:
  1. "تم مراجعة جميع صور المنتج"
  2. "تم اضافة كاتيجوري فئة الشوز + كاتيجوري البراند"
- زرار "✅ تأكيد المراجعة" جوه النافذة **معطّل** لحد ما الاتنين يتأكدوا
  (`updateReviewConfirmBtn()`). الضغط عليه بينادي `markProductReviewed()` نفسها
  (نفس المنطق اللي كان بيتنفذ مباشرة من onclick الزرار قبل كده) — هي اللي
  بتفتح خطوة ③ لأول مرة. زرار "إلغاء" أو الضغط برّه النافذة بيقفلها من غير
  ما يفتح خطوة ③.
- راجع `openReviewConfirmModal()`/`closeReviewConfirmModal()`/
  `toggleReviewCheck()`/`confirmProductReviewed()` في `index.html`.

### 2) صندوق "تعديلات أخرى تحدث تلقائي عند الربط" — نقل لآخر الخطوة + بند جديد

- الصندوق (`.opt-block` عرض فقط، v2.9.0) كان أول حاجة في خطوة ③ فوق الأسئلة
  التلاتة — دلوقتي بقى **آخر حاجة** في الخطوة، بعد سؤال "تحديث السعر" مباشرة
  وقبل زرار "ابدأ ربط المنتج".
- بند جديد اتضاف تحت "على Stylebox.online": **"التأكد من ضبط رابط المنتج
  (URL/Slug) ليطابق عنوانه"** — توثيق لسلوك `slugify()`/`slugFixed` الموجود
  فعلاً في `index.js` من v2.6.0 (كان موصوف في CLAUDE.md بس مش ظاهر في نص
  الصندوق نفسه في الواجهة).

### 3) خطوة ④ "النتيجة" — عنوان جدول المقاسات + مكان جدول فرق السعر

- عنوان جدول الـ Variations بقى **"📦 المقاسات: ✓ تمت (N) | ⚠ تحذير (N) |
  تخطي (N)"** بدل "📦 الـ Variations — ✓ N تمت | ⚠ N تحذير | تخطي N — من أصل
  N" (اتشالت "من أصل N" من الصيغة).
- سطر "فرق السعر المستخدم" (لو `priceApplied`) اتنقل من فوق جدول الـ
  Variations لجدول `<table class="variant-table">` مستقل واضح بصريًا (عنوان +
  صف قيمة) **تحت** الجدول مباشرة.

## حارس "اتربط قبل كده" + مراجعة إلزامية + إعادة ترتيب النتيجة — Worker v2.7.0 · واجهة v2.9.0 (27-08-2026)

بطلب صريح من صاحب الأداة — خمس تعديلات، بند واحد بس فيه منطق فعلي جديد في
الـ Worker (البند 1)، الباقي UI بحت في `index.html`.

### 1) حارس "اتربط قبل كده" — أول حاجة في `find_product`، على مستوى الـ Worker

- قبل أي بحث في ووكومرس، الـ Worker بيتحقق من metafield
  `custom.wordpress_id` على منتج شوبيفاي المطلوب (نفس الـ metafield اللي
  `syncProduct` بيكتبها في خطوة 2 من ترتيب التنفيذ). لو فيها قيمة (مش
  فاضية)، يبقى المنتج ده اتربط قبل كده.
- **الرد في الحالة دي:** `{ok:true, alreadyLinked:true, wordpressId,
  productTitle}` — الواجهة بتعرض رسالة **"هذا المنتج تم ربطه من قبل"** وما
  بتفتحش خطوة الربط خالص (خطوة ③ بتفضل مخفية).
- لو المنتج لسه مش مربوط (`alreadyLinked:false`)، البحث بيكمل زي ما كان
  بالظبط (فلتر GTIN → بحث بعنوان شوبيفاي لتضييق النطاق → تأكيد قاطع بالـ
  SKU/GTIN — راجع `findWcProductByShopifyId()`).
- راجع `checkShopifyAlreadyLinked()`/`PRODUCT_WPID_CHECK_QUERY` في
  `index.js` §FIND.

### 2) زرار مراجعة إلزامي في خطوة ② "نتيجة البحث" — واجهة فقط

- اتشال صف SKU وصف "اتلقى عن طريق" من نتيجة البحث.
- زرار "عرض المنتج على وردبريس للمراجعة" اتغيّر اسمه لـ**"راجع صور +
  كاتيجوريز المنتج قبل الربط"** وبقى **إلزامي الضغط عليه**: خطوة ③ (الربط)
  ما بتظهرش خالص إلا بعد الضغط عليه — `markProductReviewed()` هي اللي
  بتفتحها. `runSync()` كمان بيرفض التشغيل لو `productReviewed !== true`
  (دفاع مزدوج، زي أي حارس تاني في الأداة).

### 3) نافذة "البراند غير موجود" — حذف جملة

- اتشالت جملة "(Vendor على شوبيفاي)" من نص رسالة النافذة — النص بقى "لا
  يوجد براند بنفس اسم X على ووردبريس..." من غير الجملة المعترضة دي.

### 4) قسم "يحصل دائمًا بدون خيار" — إعادة تصميم بنفس شكل الأسئلة التانية

- بقى صندوق `.opt-block` بنفس تصميم باقي أسئلة خطوة ③ بالظبط (عرض فقط،
  بدون أي اختيار)، بعنوان جديد **"تعديلات أخرى تحدث تلقائي عند الربط:"**
  (بدل "يحصل دائمًا في كل تشغيلة — بدون أي اختيار:")، مقسّم لقسمين:
  - **على Stylebox.online:** تغيير حالة المنتج إلى Published، إضافة
    البراند مثل الـ Vendor على شوبيفاي، مزامنة كمية المخزون لكل مقاس/SKU.
  - **وعلى Ecommoda.com:** إضافة Wordpress IDs على المنتج وعلى كل مقاس،
    إضافة تاج `stylebox` على شوبيفاي.

### 5) خطوة ④ "النتيجة" — إعادة ترتيب + تعديل نص

- بانر "✅ تم الربط بنجاح" وزرار "🆕 بدء منتج جديد" اتنقلوا لآخر الخطوة —
  بعد تفاصيل بيانات المنتج والـ Variations، مش قبلها.
- زرار "بدء منتج جديد" بقى زرار كامل عرض الصفحة لوحده (اتشال النص
  التوضيحي اللي كان جنبه في `.new-product-bar` القديمة).
- في سطر ملخّص البانر: كلمة "Variation" اتغيّرت لـ**"مقاس"**، وكلمة "تمت"
  اتغيّرت لـ**"تمت المزامنة"**.

## حارس البراند + تصحيح الـ Slug — v2.6.0 (27-08-2026)

بطلب صريح من صاحب الأداة — تعديلان جديدان في منطق `sync_product` نفسه (على
عكس v2.6.0/v2.7.0 اللي فوق، اللي كانت UI بحت). **الاتنين في الـ Worker
(`index.js`) — الواجهة (v2.8.0) بتعرض النتيجة/الخطأ بس، صفر منطق جديد
فيها.**

### 1) حارس براند إلزامي — قبل أي كتابة في `syncProduct`

- الـ Vendor على شوبيفاي (حقل `vendor` على المنتج) لازم يكون له براند بنفس
  الاسم بالظبط (مطابقة حرفية case-insensitive بعد trim) على تاكسونومي
  `product_brand` في ووردبريس — نفس التاكسونومي اللي شاشة تحرير المنتج على
  ووردبريس بتاعته فيها "All Brands" checkboxes + "+ Add New Brand"
  (`/wp-admin/edit-tags.php?taxonomy=product_brand&post_type=product`).
- **لو مفيش براند مطابق: الربط بالكامل بيتوقف من غير أي كتابة على أي منصة**
  (لا شوبيفاي ولا ووكومرس اتلمسوا) — رد مُبنيَن من الـ Worker
  `{ok:false, code:'brand_missing', vendor, addBrandUrl}` بـ HTTP 409. الواجهة
  بتعرض نافذة خطأ فيها زرار **"➕ إضافة البراند على StyleBox"** بيفتح صفحة
  إضافة البراند مباشرة في تاب جديد.
- **لو موجود:** بيتربط بالمنتج تلقائيًا (`brands:[{id}]`) في نفس نداء
  `status=publish` (خطوة 3 تحت) — مفيش نداء PUT إضافي.
- الـ Vendor فاضي على شوبيفاي = الحارس بيتخطّى تمامًا (مفيش حاجة تتطابق
  أصلاً) — مش اعتباره "براند موجود".
- راجع `BrandNotFoundError`/`wcFindBrandByName()`/`wcSearchBrands()` في
  `index.js`.

⚠️ **لسه مش مؤكَّد فعليًا** إن `/wc/v3/products/brands` REST endpoint شغّال
على `stylebox.online` (زي فلتر `global_unique_id` قبل ما يتأكد فعليًا
26-08-2026) — الافتراض إنه تاكسونومي البراندات الأصلي في ووكومرس 9.4+ بناءً
على شكل الشاشة في السكرين المرفق مع الطلب (نفس تصميم Categories/Tags +
"+ Add New Brand"). راجع "مسائل مفتوحة" تحت.

### 2) تصحيح الـ Slug — إلزامي بدون خيار

- عند كل `sync_product`، لو الـ `slug` بتاع منتج ووردبريس مش مطابق
  للـ slug المتوقع من عنوانه (`slugify(wooProduct.name)` — أحرف صغيرة،
  استبدال أي حرف مش a-z0-9 بشرطة، دمج الشرطات المتتالية، قص الشرطات من
  الطرفين)، بيتصلّح تلقائيًا في نفس نداء `status=publish` (خطوة 3 في ترتيب
  التنفيذ تحت) — مفيش نداء PUT إضافي.
- مثال: `The North Face Vectiv Enduris 3 - in Beige` →
  `the-north-face-vectiv-enduris-3-in-beige`.
- مفيش خيار لتعطيل الخطوة دي — زي نشر ووردبريس (`status=publish`) بالظبط.
- العنوان المصدر هو عنوان **ووكومرس** (`wooProduct.name`) — مش عنوان شوبيفاي
  (اللي ممكن يكون فيه "⭐ " مُضافة من الأداة نفسها).
- راجع `slugify()`/`slugFixed` في `index.js`.

## تعديلات UI إضافية — v2.7.0 (27-08-2026)

جولة تعديلات ثانية بطلب صاحب الأداة على نفس إعادة التصميم (v2.6.0 تحت).
**برضه تعديل UI بحت — صفر تغيير على `index.js` أو أي `action` أو أي payload.**

- **"نتيجة البحث" بقت خطوة ② مستقلة لوحدها** (كانت جوه خطوة البحث نفسها) —
  الخطوات بقت أربعة: ① بحث → ② نتيجة البحث → ③ الربط وتحديث السعر → ④ النتيجة.
- **زرار "🔗 عرض المنتج على وردبريس للمراجعة"** وزرار الربط النهائي بقوا
  `.btn-block` — زرار كبير كامل عرض الصفحة (زي زرار "تحديث الكل" في أدوات
  تانية) بدل زرار صغير. زرار الربط اتغيّر اسمه من "▶ تشغيل الربط" لـ
  **"▶ ابدأ ربط المنتج بين الموقعين الآن"** ولونه بقى أخضر (`.success`).
- **أسئلة الربط التلاتة (الحالة/النجمة/السعر) بقت صناديق `.opt-block`
  مستقلة بإطار**، عنوان السؤال والاختيارات في نفس الصف (مش فوق بعض) —
  بدل ما تكونوا مجرد فقرات متتالية.
- **شيبس "ⓘ تفاصيل" (الحالة/النجمة) وشيبس "ⓘ إزاي بيتحسب السعر؟" اتشالوا
  بالكامل** بطلب صريح من صاحب الأداة.
- **جملة "بيحصل دايمًا بدون خيار" بقت صندوق ثابت `.always-box` ظاهر
  دايمًا** (🔒) — مش شيبس قابلة للطي زي باقي الأسئلة، عشان توضّح إنه إجراء
  إجباري مش سؤال محتاج إجابة من المستخدم.
- **خطوة ① بقت تقبل رابط منتج شوبيفاي بالكامل** (من صفحة المنتج على أدمن
  شوبيفاي) مش الرقم الصريح بس — `extractShopifyProductId()` بيستخرج الرقم
  من الرابط على مستوى الواجهة. القيمة اللي بتتبعت لـ `find_product` لسه
  رقم صريح زي الأول، صفر تغيير في العقد مع الـ Worker.
- **إصلاح تنبيه "⚠️ نسخة الـ Worker مختلفة"** — كان بيقارن نسخة الواجهة
  (`TOOL_VERSION`) مباشرة بنسخة الـ Worker، فكان بيطلع تحذير زائف كل مرة
  تتعدّل فيها الواجهة بس (زي جولة v2.6.0/v2.7.0 دي) من غير أي لمس للـ
  Worker — رغم إن الاتنين نسختين مستقلتين زي ما موثّق فوق. دلوقتي بيقارن
  بثابت جديد `EXPECTED_WORKER_VERSION = 'v2.5.0'` (نسخة الـ Worker اللي
  الواجهة اتبنت عليها فعليًا) — الثابت ده بيتحدّث يدويًا **بس لو `index.js`
  نفسه اتغيّر فعلاً وترقّى على Cloudflare**، مش مع كل تعديل واجهة.

## إعادة تصميم الواجهة (UI فقط) — v2.6.0 (27-08-2026)

بطلب صريح من صاحب الأداة — الشكل القديم كان "بدائي" وخطوطه صغيرة ومليان
شروحات ظاهرة دايمًا. **تعديل UI بحت على `index.html`، صفر تغيير على
`index.js` أو أي `action` أو أي payload.**

- خطوات "ربط منتج" بقت بطاقات مرقّمة (① بحث → ② ربط + سعر → ③ نتيجة) —
  كل خطوة بتتطوى تلقائيًا وهي بتتنقل للتالية (زي طلب المستخدم: "حاجات
  المفروض تكون Collapsed لما تنتقل من مرحلة لمرحلة")، وتفتح تاني بالضغط
  على عنوانها.
- خط ومسافات أكبر في كل مكان، أزرار أكبر وأوضح (`.btn-primary` بقى بارز
  بظل وhover)، وشروحات الأسئلة الطويلة بقت اختيارية خلف زرار "ⓘ تفاصيل"
  بدل ما تكون ظاهرة دايمًا (كانت المشكلة اللي المستخدم سمّاها "مليانة
  Placeholders بدون داعي").
- شاشة النتيجة (خطوة ③) اتعاد تصميمها بالكامل: بانر حالة كبير ملوّن
  (أخضر/أصفر/أحمر) بدل عرض النتيجة كبطاقة عادية زي باقي الأداة.
- زرار جديد **"🆕 بدء منتج جديد"** أعلى خطوة النتيجة — بيصفّر كل حاجة
  (البحث + الخيارات + النتيجة) ويرجّع الأداة لخطوة ① من غير Refresh —
  ده كان ناقص تمامًا قبل كده.
- راجع `§TOOL-JS::steps` في `index.html` (`toggleStep`/`setStepCollapsed`/
  `toggleHint`/`startNewProduct`) والقسم الأول في About modal للتفاصيل.

## ترتيب التنفيذ في `sync_product` (مقصود — متغيّرهوش من غير سبب)

```
1. قراءة منتج ووكومرس + الـ Variations + منتج شوبيفاي
1.5. ⚠️ حارس إلزامي (v2.6.0) : لازم براند على ووردبريس بنفس اسم الـ Vendor
     على شوبيفاي — وإلا الربط بالكامل يتوقف من غير أي كتابة (BrandNotFoundError)
2. Shopify product-level : status (حسب الخيار) + ⭐ (حسب الخيار) + metafield wordpress_id
3. WooCommerce product-level : status='publish' (دايمًا، بدون خيار) + meta _shopify_product_id
   + slug fix (v2.6.0، دايمًا بدون خيار) + ربط الـ Brand (v2.6.0، لو الخطوة 1.5 لقت تطابق)
4. لكل Variation : SKU/مخزون/meta على ووكومرس + wordpress_variation_id على شوبيفاي
5. tagsAdd("stylebox") فورًا ← آخر خطوة على الإطلاق (بدون أي انتظار)
```

الخطوة 1.5 بتوقف العملية بالكامل لو فشلت (زي فشل `no global_unique_id`) —
مش معزولة في `try/catch` مستقل زي 2/3/5. الخطوات 2 و3 و5 كل واحدة معزولة في
`try/catch` لوحدها — فشل واحدة مايوقفش التانية.

> تعديل 26-08-2026 (v2.2.1): الانتظار كان 5 ثواني، اتغيّر لـ 10 ثواني بناءً على طلب
> صاحب الأداة. راجع `TAG_DELAY_MS` في `index.js` (وقتها).
>
> ⚠️ تعديل 26-08-2026 (v2.4.0): الانتظار (`TAG_DELAY_MS`) **اتلغى بالكامل** —
> بطلب صريح من صاحب الأداة نفس اليوم. `tagsAdd` لسه بيتنفّذ **آخر خطوة على
> الإطلاق** زي ما هو (الترتيب نفسه ما اتغيّرش) — الملغي هو الانتظار العشر
> ثواني قبله بس. الثابت `TAG_DELAY_MS` اتشال تمامًا من `index.js`.

### GTIN fallback من الـ SKU (v2.2.0 — 26-08-2026)

في الخطوة 1، لو `global_unique_id` (خانة GTIN) فاضية على منتج ووكومرس، الـ
Worker بيدوّر على رقم شوبيفاي في **بداية** الـ `SKU` (نمط `رقم-slug`، زي
`10468835819842-skechers-slip-ins-…`، حد أدنى 6 أرقام). لو لقاه:
يكتب الرقم في `global_unique_id`، ويمسحه من الـ SKU (يفضل الباقي بس كـ SKU
جديد)، ويكمّل بقية `sync_product` عادي بنفس الرقم. لو مفيش رقم في الـ SKU
برضه، الفشل زي ما كان (`no global_unique_id ... — skipping`).
راجع `extractGtinFromSku()` في `index.js`.

## أكشن `find_product` (v2.4.0 — خطوة 1 في الواجهة، بحث بالعنوان اتضاف v2.5.0، حارس "اتربط قبل كده" اتضاف v2.7.0)

الواجهة اتعاد تصميمها بالكامل (v2.4.0) على خطوتين: الخطوة 1 بحث، الخطوة 2 ربط
+ تحديث سعر. `find_product` هو أكشن الخطوة 1 — **قراءة بس، مفيش كتابة ومفيش D1
log** (زي `diag`/`get_config`). مربع الإدخال بيستجيب لـ Enter كمان مش زرار
البحث بالماوس بس (اتصلّح v2.5.0).

```
GET ?action=find_product&shopify_product_id=10468878713154
```

### 0) حارس "اتربط قبل كده" — أول حاجة بتتنفّذ (v2.7.0)

قبل أي بحث في ووكومرس، الـ Worker بيتحقق من metafield `custom.wordpress_id`
على منتج شوبيفاي المطلوب. لو فيها قيمة (مش فاضية)، يبقى المنتج ده اتربط قبل
كده — الرد بيرجع `{ok:true, alreadyLinked:true, wordpressId, productTitle}`
من غير ما يكمّل بحث WC خالص. الواجهة بتعرض "هذا المنتج تم ربطه من قبل" ووقف
— خطوة الربط ما بتظهرش. لو `alreadyLinked:false`، البحث بيكمل زي القسم تحت
بالظبط. راجع `checkShopifyAlreadyLinked()`/`PRODUCT_WPID_CHECK_QUERY`.

- الموظف بيدخل **رقم منتج شوبيفاي الرقمي** (من رابط المنتج على أدمن شوبيفاي،
  زي `.../products/10468878713154`) — مش رقم منتج ووكومرس زي قبل كده.
- الـ Worker بيدوّر على منتج ووكومرس اللي يطابقه: إما `global_unique_id`
  (GTIN) بيساوي الرقم، أو الرقم في **بداية** الـ SKU (نفس ريجيكس
  `extractGtinFromSku`، لكن بالعكس — هنا الرقم معروف من الأول وبندوّر بيه على
  المنتج).
- **تلات محاولات لتجميع مرشّحين (كانت اتنين قبل v2.8.0)، لكن القبول النهائي
  بيعتمد على GTIN/SKU بس أبدًا**:
  1. فلتر `global_unique_id` مباشر على `/wc/v3/products` — رسمي في ووكومرس
     9.2+ (نفس الحقل الظاهر في خانة "GTIN, UPC, EAN, or ISBN" في محرر
     المنتج). ✅ **مؤكَّد شغّال فعليًا على `stylebox.online`** (تجربة حقيقية
     26-08-2026 — Ahmed دخل الرقم يدويًا في خانة GTIN لمنتج والبحث لقاه فورًا).
  2. **(v2.5.0، اتحصّن v2.8.0)** بحث بعنوان المنتج على شوبيفاي (`search=`) —
     الـ Worker بيجيب عنوان المنتج من شوبيفاي بالـ GraphQL الأول، بعدين يبحث
     بيه في ووكومرس. ⚠️ **العنوان هنا لتضييق نطاق البحث بس، مش تأكيد نهائي** —
     ممكن منتجين يتشابهوا في العنوان، أو العنوان يختلف جزئيًا بين المنصتين.
     **من v2.8.0** العنوان بيتنضّف (أي تتابع مسافات → مسافة واحدة) وبيتبعت منه
     **أول `TITLE_SEARCH_WORDS` (=4) كلمات بس** بدل العنوان كامل — راجع
     `buildTitleSearchQuery()`.
  3. **(v2.8.0) مسح احتياطي بالـ SKU** (`wcScanProductsBySkuPrefix()`) —
     **بيشتغل بس لو 1 و2 رجّعوا صفر مرشّحين**. بيمشي على منتجات ووكومرس صفحة
     صفحة (`per_page=100`، `orderby=date&order=desc`، `_fields` عشان الرد خفيف)
     وبيطابق محليًا على بداية الـ SKU، وبيقف فورًا أول ما يلاقي. **مستقل تمامًا
     عن العنوان** — بيغطي أي اختلاف مهما كان.
  > ⚠️ **المحاولة القديمة (v2.4.0) كانت بحث نصي بالرقم نفسه (`search=idStr`) —
  > اتشالت v2.5.0.** تجربة حقيقية (26-08-2026) أثبتت إن ووكومرس هنا **مبيدوّرش
  > بيها على الـ SKU خالص** — رجّعت صفر مرشّحين حتى لمنتج SKU بتاعه بيبدأ
  > حرفيًا بالرقم المطلوب (`10468878713154-the-north-face-…`). لو حد شاف
  > الكود القديم بيستخدم `search=` بالرقم، ده اللي كان بيحصل. **المحاولة 3
  > الجديدة هي البديل الصحيح لده** — مسح مباشر بدل الاعتماد على بحث ووكومرس.
- لو لقى تطابق، بيرجّع `wp_product_id` + `productName` + `sku` + `matchedBy`
  (`gtin`/`sku`) + `matchedVia` (`search`/`scan`، v2.8.0) + `wpEditUrl` (رابط
  صفحة تعديل المنتج جاهز، مبني من `WC_BASE_URL` — مفيش سر جديد مطلوب). الواجهة
  بتعرض زرار "🔗 راجع صور + كاتيجوريز المنتج قبل الربط" بيه، وبتفتح الخطوة 2
  (خيارات الربط + تحديث السعر) بالـ `wp_product_id` ده جاهز — الموظف مبقاش
  بيكتبه يدوي خالص.
- لو مفيش تطابق، بيرجّع `{ found: false, scanned: N, scannedAll: bool }` (مش
  خطأ — حالة متوقعة). **`scannedAll:false` (v2.8.0) معناها المسح وقف عند سقف
  الصفحات (`SKU_SCAN_MAX_PAGES = 30` = 3000 منتج) أو فشل — يعني "مش لقيته" مش
  قاطعة**، والواجهة بتقول للموظف كده صراحةً. لو حصلت، الحل اليدوي البديل: افتح
  المنتج على ووردبريس وحط الرقم في خانة GTIN بنفسك، والبحث هيلاقيه فورًا عن
  طريق فلتر GTIN (المحاولة 1).

راجع `findWcProductByShopifyId()`/`wcSearchProducts()`/`wcScanProductsBySkuPrefix()`/
`buildTitleSearchQuery()`/`wcProductMatchesShopifyId()` في `index.js`.

## خيارات `sync_product` (v2.1.0 — فرق السعر اتضاف v2.3.0 كأكشن منفصل، واندمج هنا v2.5.0)

| البراميتر | القيم | الافتراضي |
|---|---|---|
| `shopify_status` | `ACTIVE` · `DRAFT` · `KEEP` (ما تبعتش `status` خالص) | `DRAFT` |
| `add_star` | `true` · `false` | `true` |
| `price_difference` | رقم (يقبل سالب) أو مفيش خالص | مفيش — تحديث السعر يتخطّى تمامًا |

> `skip_draft` / `skip_star` القديمين لسه مقبولين كـ fallback في الـ Worker
> (`skip_draft:true` ≡ `KEEP`، `skip_star:true` ≡ `add_star:false`) عشان أي واجهة
> قديمة في كاش المتصفح ماتكسرش. الواجهة الحالية مبتبعتهمش خالص.

### فرق السعر (`price_difference`) — v2.5.0: اندمجت جوه `sync_product`

> ⚠️ **كان أكشن منفصل تمامًا `update_price` (v2.3.0، 26-08-2026) — اتشال
> بالكامل واندمج جوه `sync_product` نفسها v2.5.0 (نفس اليوم) بطلب صريح من
> صاحب الأداة: "متكونش خطوة منفصلة بعد الربط، المفروض تكون جزء من عملية
> الربط نفسها، زي إضافة النجمة للعنوان مثلاً."** لو لقيت أي إشارة لأكشن
> `update_price` في كود قديم أو نسخة محفوظة، ده بقى تاريخي بس.

- `price_difference` **اختياري تمامًا** — لو مبعوتش/فاضي، خطوة تحديث السعر
  بتتخطّى تمامًا (سلوك `sync_product` القديم قبل v2.3.0 زي ما هو، بدون أي أثر).
  مستقل تمامًا عن `shopify_status`/`add_star` — قيمة واحد مالهاش أي أثر
  على التانيين، بالظبط زي علاقة الحالة بالنجمة.
- بييجي من حقل في الواجهة مباشرة، **مش** `env var` زي `PRICE_DIFFERENCE` في
  أداة "مزامنة أسعار Stylebox". القيمة بتتحفظ في `localStorage`
  (`stylebox_link_price_difference`) كآخر قيمة استُخدمت **للراحة بس** — مفيش
  قيمة افتراضية مبيّتة في الكود، ومفيش إرسال تلقائي من غير ما المستخدم يضغط
  "تشغيل الربط".
- **الكتابة بتحصل في نفس نداء `wcUpdateVariation` بتاع SKU/المخزون** (خطوة 4
  في ترتيب التنفيذ فوق) — مش نداء إضافي، ومش خطوة منفصلة زمنيًا. `regular_price`/
  `sale_price` بيتحطّوا في نفس الـ payload.
- **المطابقة بالمقاس** — نفس آلية باقي `sync_product` (`findWcSize`/
  `findShopifyVariantBySize`)، **مش** `wordpress_variation_id`/GTIN triple-check
  بتاع أداة "مزامنة أسعار Stylebox".
- **معادلة الحساب** (نفس معادلة `stylebox-price-sync-worker` حرفيًا —
  `computeVariantPrices()`): لو فيه خصم فعلي على شوبيفاي
  (`compareAtPrice > price`) → `regular_price = compare_at + diff` و
  `sale_price = price + diff`. وإلا → `regular_price = price + diff` و
  `sale_price` بيتفضّى (بيمسح أي خصم قديم على ووكومرس).
- **مفيش GTIN-from-SKU fallback خاص بالسعر** — لو المنتج لسه مش مربوط
  (`global_unique_id` فاضي)، العملية كلها بترفض زي أي `sync_product` تاني
  (الـ fallback العادي بتاع `syncProduct` بيتطبّق زي ما هو، مفيش استثناء).
- **D1:** بيستخدم نفس `type = 'synced'`/`'error'` الأصليين لكل variation —
  **مفيش `type` جديد ولا `extra.operation` منفصل** (كان `extra.operation =
  'price_update'` في v2.3.0 — اتشال). المُميِّز دلوقتي `extra.priceApplied = true`
  في صف الـ variation لو الخطوة دي اتنفذت.

## الروابط

```
الواجهة    : https://ecommoda-dev.github.io/StyleBox-Products-Linking/
الـ Worker : https://stylebox-products-linking-worker.ecommoda-dev.workers.dev
اسم الـ Worker في الداشبورد: stylebox-products-linking-worker     ← لازم يطابق name في wrangler.toml
```

## الـ Endpoints

| `?action=` | بيعمل إيه |
|---|---|
| `find_product` | البحث بمعرف شوبيفاي (v2.4.0، بحث بالعنوان اتضاف v2.5.0) — خطوة 1، GET، قراءة بس (`shopify_product_id` مطلوب). ⚠️ (v2.7.0) أول حاجة بيتحقق هل المنتج اتربط قبل كده (`custom.wordpress_id` مش فاضي) ويرجع `{alreadyLinked:true, wordpressId}` لو آه — راجع القسم فوق |
| `find_product_relink` | **(v2.9.0)** نسخة "إعادة الربط" من `find_product` — بيستخدمها تاب Bulk بس. المنتج المربوط قبل كده مش سبب رفض: رقم ووردبريس بيتاخد من `custom.wordpress_id` مباشرةً (مفيش بحث WC). لو الميتافيلد بيشاور على منتج بيخص رقم شوبيفاي تاني → `{found:false, conflict:true}` من غير أي كتابة. منتج مش مربوط → بحث عادي. قراءة بس، مفيش D1 log |
| `sync_product` | العملية الأساسية — ربط/مزامنة منتج واحد (POST، `wp_product_id` مطلوب، + `shopify_status`/`add_star`/`price_difference` اختياريين — راجع القسم فوق). ⚠️ (v2.6.0) ممكن يرجع HTTP 409 `{code:'brand_missing', vendor, addBrandUrl}` لو مفيش براند مطابق على ووردبريس |
| `check_employee` / `register_pin` / `verify_employee` / `log_logout` / `get_employees` | Universal D1 Auth |
| `diag` | تشخيص env/Shopify/WC/D1 |
| `get_config` | نسخة الـ Worker |
| `get_logs` / `get_logs_count` / `get_logs_export` | سجل العمليات |

## D1

```
tool  : stylebox_products_linking
type  : product_meta_synced · synced · error · login · logout
```

> لو القيم دي مش في جدول D1 في `ecommoda-constants` §7 → ضيفها هناك الأول.

> ⚠️ **تحديث السعر (`price_difference` جوه `sync_product`) بيستخدم نفس
> `type = 'synced'`/`'error'` فوق — مفيش `type` جديد.** المُميِّز
> `extra.priceApplied = true` جوه صف الـ variation (كان `extra.operation =
> 'price_update'` وقت ما كان أكشن `update_price` منفصل في v2.3.0 — اتشال
> مع دمج الميزة v2.5.0). قرار مقصود (مش سهو): الجلسة اللي بنت الميزة دي
> معندهاش وصول لريبو `ecommoda-constants` عشان تسجّل `type` جديد فيه قبل أول
> استخدام (Rule 7). لو محتاج فلترة منفصلة لعمليات تحديث السعر في تاب السجل
> مستقبلًا، سجّل `type` مخصّص (مثلاً `price_synced`) هناك **الأول** قبل أي
> تعديل تاني.

## المضبوط فعليًا في الداشبورد

> اللي **متظبط بالفعل** — مش اللي المفروض يكون.

```
Bindings : DB → ecommoda-dev-logs
Secrets  : WORKER_SECRET · CLIENT_ID · CLIENT_SECRET   ← Shopify OAuth بس
           (الأداة دي مبتكلّمش WooCommerce REST مباشرة عبر أسرار WC — راجع index.js
            لو WC_BASE_URL/WC_CONSUMER_KEY/WC_CONSUMER_SECRET اتفعّلوا فعليًا)
Vars     : SHOP_DOMAIN   ← من [vars] في wrangler.toml. مفيش LOCATION_ID (مش أداة مخزون)
Build watch paths : * الافتراضي
```

## CORS

`ALLOWED_ORIGINS` صارمة (`https://ecommoda-dev.github.io` فقط) — Option B، لأن
الأداة **كتابة**: بتغيّر حالة/عنوان/تاج المنتج على شوبيفاي ومخزون/SKU على WooCommerce.

## خط الأساس بعد النقل

> الأرقام اللي الأداة رجّعتها بعد ما اتأكدنا إنها شغالة — مرجع لأي شك بعد كده.

```
لسه محتاج تسجيل بعد أول اختبار حقيقي بعد الربط في Cloudflare.
```

## فخاخ الأداة دي

- الأداة دي **manual-only بالتصميم** — مفيش `scheduled()` ومفيش `sync_all`. أي
  إضافة Cron أو دفعة تشغيل جماعي = مخالفة مقصودة، راجع تعليق §CONSTANTS في
  `index.js` قبل ما تضيفها.
- `shopify_status` و`add_star` خياران **مستقلّان تمامًا** عن بعض — قيمة واحد
  مالهاش أي أثر على التاني. لا تدمجهم في خيار واحد.
- الـ Tag `stylebox` **لازم يفضل آخر خطوة** في `syncProduct` — ده طلب صريح من
  صاحب الأداة مش تفصيلة تنفيذية. أي نقل ليه لأول العملية = مخالفة مقصودة.
  ⚠️ **الانتظار العشر ثواني اللي كان قبله (`TAG_DELAY_MS`) اتلغى بالكامل
  v2.4.0** — القاعدة القديمة ("الحذف للانتظار = مخالفة") اتقلبت: صاحب الأداة
  نفسه طلب الإلغاء 26-08-2026. لو رجّعت الانتظار من غير طلب صريح جديد، ده
  اللي بقى مخالفة دلوقتي.
- الواجهة (v2.4.0) بقت **خطوتين إلزاميتين**: البحث بمعرف شوبيفاي (`find_product`)
  الأول، وبعده الربط/تحديث السعر — مفيش إدخال يدوي لـ WooCommerce Product ID
  خالص، لازم يجي من نتيجة البحث. أي رجوع لإدخال يدوي بدون البحث = مخالفة
  مقصودة لهذا التصميم.
- نشر المنتج على ووردبريس (`status=publish`) **بدون خيار** — بيحصل في كل تشغيلة.
  التأكيد بيتقرا من رد ووكومرس نفسه (`status === 'publish'`) مش من HTTP 200.
- كل عملية `sync_product` لازم تكون من موظف مسجّل دخول (Universal D1 Auth) —
  مفيش استثناء "تشغيل يدوي بدون تسجيل دخول" هنا رغم إن الأداة manual-only.
- **(v2.6.0) حارس البراند بيوقف العملية بالكامل** — لو الـ Vendor على شوبيفاي
  معندوش براند مطابق على ووردبريس، الحارس ده **قبل** أي كتابة (حتى قبل
  الخطوة 2 Shopify product-level) — مش زي فشل باقي البلوكات (2/3/5) اللي كل
  واحد معزول في `try/catch` مايوقفش التانية. أي طلب "خلّي الربط يكمل حتى لو
  البراند مفقود" = رجوع عن تصميم مقصود صريح — يحتاج طلب جديد.
- **(v2.6.0) تصحيح الـ Slug بدون خيار** — بيحصل في كل تشغيلة زي
  `status=publish` بالظبط، مفيش parameter لتعطيله. العنوان المصدر هو عنوان
  ووكومرس (`wooProduct.name`) — مش عنوان شوبيفاي (اللي ممكن يحمل "⭐ ").
- تحديث السعر (`price_difference`) **جزء من `sync_product` نفسها من v2.5.0** —
  مفيش أكشن `update_price` منفصل خالص (اتشال بالكامل، كان موجود في v2.3.0
  بس). أي طلب لفصله تاني عن الربط = رجوع لتصميم قديم اتشال عمدًا بطلب صاحب
  الأداة — يحتاج طلب صريح جديد.
- فرق السعر (`price_difference`) بيتحفظ في `localStorage` **للراحة بس** (آخر
  قيمة استُخدمت) — مفيش قيمة افتراضية صامتة في الكود، ولازم المستخدم يدخلها
  ويضغط "تشغيل الربط" في كل مرة. لو حد غيّر القيمة في الحقل ونسي، مفيش هامش
  "افتراضي" بيتطبّق بدل منها زي `getPriceDiff()` في أداة الأسعار التانية.
  حقل فاضي = تخطّي تحديث السعر تمامًا، مش صفر.
- **(v2.7.0) حارس "اتربط قبل كده" في `find_product` بيوقف البحث بالكامل** —
  لو `custom.wordpress_id` على شوبيفاي مش فاضي، الـ Worker بيرجّع
  `alreadyLinked:true` من غير ما يكمّل بحث ووكومرس خالص (مش نفس منطق GTIN/SKU
  تحت). أي طلب "كمّل البحث حتى لو المنتج مربوط" = رجوع عن تصميم مقصود صريح.
- **(v2.9.0) زرار "راجع صور + كاتيجوريز المنتج قبل الربط" إلزامي** — خطوة ③
  (الربط) ما بتظهرش في الواجهة إلا بعد الضغط عليه (`markProductReviewed()`)،
  و`runSync()` كمان بيرفض التشغيل لو `productReviewed !== true`. أي طلب
  "خلّي خطوة الربط تظهر تلقائي زي قبل v2.9.0" = رجوع عن تصميم مقصود.
- **(v2.10.0) نافذة تأكيد المراجعة (بندين checkbox) إلزامية قبل ما
  `markProductReviewed()` تتنفّذ** — الزرار بقى بينادي `openReviewConfirmModal()`
  مش `markProductReviewed()` مباشرة؛ زرار "تأكيد المراجعة" جوه النافذة معطّل
  لحد ما الاتنين البندين يتأكدوا. أي طلب "رجّع الزرار يفتح خطوة ③ على طول
  من غير نافذة" = رجوع عن تصميم مقصود.
- **(v2.11.0) الأسئلة التلاتة في خطوة ③ من غير أي اختيار افتراضي جاهز** —
  الحالة على شوبيفاي والنجمة كانوا Draft/نعم محددين تلقائيًا، دلوقتي محدش
  منهم محدد لحد ما الموظف يضغط بنفسه، وسؤال فرق السعر بقى ليه زرار "تأكيد
  فرق السعر" إلزامي منفصل عن كتابة الرقم نفسه. `runSync()` بيرفض التشغيل لو
  أي سؤال من التلاتة من غير إجابة/تأكيد (`readSyncOptions()` بترجع `null`
  للي مش مختار، و`priceDiffConfirmed` لازم يكون `true`). أي طلب "رجّع Draft/
  نعم يبقوا افتراضيين تاني" أو "خلّي كتابة الرقم كافية من غير الضغط على
  الزرار" = رجوع عن تصميم مقصود.
- **(v2.8.0) العنوان مايُستخدمش أبدًا كتأكيد نهائي — حتى بعد ما بقى مقصّر** —
  تقصير استعلام العنوان لأول 4 كلمات (`buildTitleSearchQuery()`) بيوسّع
  المرشّحين **عمدًا**، وده آمن **بس** لأن التأكيد النهائي حرفي بالـ GTIN/SKU
  (`wcProductMatchesShopifyId()`). أي طلب "خلّي البحث يقبل المنتج لو العنوان
  متطابق" = كسر للتصميم الأساسي، وهيربط منتجات غلط ببعض.
- **(v2.8.0) المسح الاحتياطي بالـ SKU ملاذ أخير، مش المسار الافتراضي** —
  `wcScanProductsBySkuPrefix()` بيشتغل **بس** لو فلتر الـ GTIN وبحث العنوان
  رجّعوا صفر مرشّحين. أي نقل ليه لأول العملية (قبل المحاولتين) = عشرات
  الـ subrequests في كل بحث بدل واحد أو اتنين.

- **(v2.13.0) مسار Bulk مسار إضافي — مش بديل** — المسار الفردي (تاب "🔗 ربط
  منتج") بكل حرّاسه (حارس "اتربط قبل كده" + زرار المراجعة + نافذة تأكيد
  المراجعة) **متغيّرش ولا سطر فيه**. أي طلب "وحّد المسارين" أو "شيل الحرّاس من
  المسار الفردي زي Bulk" = رجوع عن تصميم مقصود صريح.
- **(v2.13.0) `find_product_relink` هو الأكشن الوحيد اللي بيسمح بمنتج مربوط** —
  `find_product` لسه بيوقف عند `alreadyLinked:true` زي v2.7.0 بالظبط. أي طلب
  "خلّي `find_product` يسمح بإعادة الربط" = كسر للمسار الفردي، والصح إنه يستخدم
  الأكشن الجديد.
- **(v2.13.0) حارس التعارض في `find_product_relink` مايتشالش** — رقم ووردبريس
  من `custom.wordpress_id` **مش** إثبات كافٍ لوحده؛ لازم GTIN/SKU بتاع منتج
  ووكومرس يأكّد نفس رقم شوبيفاي. شيله معناه إن ميتافيلد قديم/غلط يقدر يخلّي
  الأداة تكتب مخزون وأسعار منتج على منتج تاني خالص.
- **(v2.13.0) الربط في Bulk لسه منتج واحد لكل نداء `sync_product`** — اللفّة
  في الواجهة، مش في الـ Worker. أي `sync_all` أو دفعة جوه نداء واحد = مخالفة
  لقاعدة manual-only + خطر تعدّي حد الـ subrequests بتاع Cloudflare (كل منتج
  لوحده بياخد عشرات النداءات).

- **(v2.10.0) كل نداء ووكومرس لازم يعدّي من `wcFetch()`** — نداء `fetch` مباشر
  لووكومرس في أي مكان جديد = رجوع للعطل اللي حصل 10-09-2026 (أول 429 = فشل
  نهائي، والقائمة كلها بتقع وراه).
- **(v2.10.0) الفرق بين 404 و429 مايتلغيش** — أي خطأ WC مش 404 لازم يترفع
  بنصّه للموظف. تحويله لـ"مفيش منتج" = رسالة بتشاور على البيانات والمشكلة في
  الاتصال، وده اللي ضيّع وقت في تشخيص العطل.
- **(v2.10.0) عزل المقاس في `try/catch` مايترجعش** — من غيره، مقاس واحد فاشل
  بيوقّع المنتج كله ويمنع تاج `stylebox` من الأساس، والمنتج بيفضل نُصّه متعدّل.

- **(v2.11.0) المقاسات بتتكتب في نداء batch واحد — مايترجعش لنداء لكل مقاس** —
  ده اللي بيخلي عدد النداءات لكل منتج 5-6 بدل 10-11 على متجر بيحجب عند ~30.
  ⚠️ ولأن الـ batch بترجّع **200 حتى لو مقاس فشل**، لازم كل عنصر في الرد يتقرا
  على حدة (`row.error`) — قراءة الـ HTTP status لوحده = نجاح كاذب لكل المقاسات.
- **(v2.11.0) كاش البراند إيجابي بس** — تخزين "البراند مش موجود" معناه إن
  الموظف يضيف البراند والأداة تفضل رافضة لحد ما الكاش يقع.

- **(واجهة v2.17.0) شاشة الإعدادات فيها خانة واحدة — الـ Secret** — أي رجوع
  لخانة `WORKER URL` أو `ADMIN WORKER URL` = مخالفة لـ`html-builder`
  Standards #28. الرابط ثابت في `§CONFIG`، والأدمن بانل مش مستخدم في الأداة دي
  خالص. و`LS_SECRET` هو المفتاح الوحيد في `localStorage`.

## استرجاع النسخ القديمة

> ده بديل الـ tags — دفع الـ tags ممنوع من جلسات Claude Code السحابية.

```
v2.0.0 → commit 3234fb1 (آخر commit قبل تعديلات 26-08-2026)
git checkout 3234fb1 -- index.js index.html
```

## بصمة المهارات

> الصيغة والقواعد والمهارات اللي بتدخل الجدول → `ecommoda-skill-versioning`
> Step 4. مهارة مالهاش رقم إصدار مابتدخلش الجدول.

| المهارة | الإصدار وقت آخر تعديل |
|---|---|
| ecommoda-worker-builder | v3.0.0 |
| ecommoda-html-builder | v7.0.0 |
| woocommerce-sync-helper | v1.0.0 |
| ecommoda-constants | v2.0.0 |
| shopify-graphql-helper | v2.1.0 |

> ⚠️ الصفّين الأخيرين **ماكانوش في الجدول خالص** لحد 10-09-2026 رغم إن الأداة
> بتستخدمهم فعليًا (رقم D1 · دومين المتجر · `API 2026-01` · عقد `shopifyGQL` ·
> §12 · §13) — يعني الطبقة ① بتاعة `skills-sweep` كانت **عمياء عنهم**.

آخر مطابقة: 10-09-2026 · `index.js` v2.12.0 · `index.html` v2.17.0 (جولة مطابقة المهارات — راجع القسم الأول)
🔴 معلّقة: **تسجيل `rejected` كقيمة `type` للأداة دي في `ecommoda-constants` §7.**
الجلسة اللي نفّذت جولة v2.12.0 **معندهاش وصول لريبو `ecommoda-constants`**، وجدول
`logs` مشترك بين كل أدوات الستاك فقيمة `type` غير مسجّلة بتنتج **صفوف يتيمة**
(نفس سابقة `write_external_log` في `cod-payment-center-worker`). فتقسيم `type`
بالأثر الخارجي (`worker-builder` Step 5A ⑭ — «اتوقف قبل أي محاولة = `rejected`»)
**اتنفّذ في `extra.result` بس**، والعمود `type` لسه بالقيم المسجّلة.
**الأثر العملي: صفر** — كل استعلام خط أساس بيفلتر على `extra.result` أصلاً
(§12). البند مفتوح عشان لما حد يفتح `ecommoda-constants` يسجّل القيمة ويكمّل
التقسيم، مش لأن حاجة مكسورة دلوقتي.

> سطر **🔴 معلّقة** = أي بند كاسر **معروف ومتقرر تأجيله**، بسببه.
> `— لا شيء` معناها مفيش. **بند 🔴 متأجل من غير ما يتكتب هنا = بند ضايع** —
> مفيش ملف تاني في المشروع بيتتبّعه.

## مسائل مفتوحة

- **⚠️ (10-09-2026) حد الـ requests على `stylebox.online` — الحل الجذري بره الأداة.**
  الـ 429 بيرجع **برد فاضي**، يعني من طبقة الاستضافة/WAF مش من ووكومرس (WordPress
  بيرجّع JSON دايمًا). الحد المرصود ~30 نداء، ونافذته أطول من 15 ثانية. v2.11.0
  نزّلت النداءات لكل منتج من ~10 لـ~5 (batch + كاش البراند) فالمنتجات اللي بتعدّي
  قبل الحجب اتضاعفت — **لكن ده تخفيف مش حل**. الحل النهائي: الاستضافة ترفع الحد
  أو تعمل allowlist لـ IPs بتاعة Cloudflare Workers. لحد ما ده يحصل، القوائم
  الطويلة هتتحجب في نصّها ولازم تتعاد بزرار "أعد محاولة الفاشلة".
- **⚠️ (10-09-2026) SKU مكرر على ووكومرس — شغل على ووردبريس مش على الأداة.**
  ⚠️ **اتضح إنه نمط مش حالة واحدة** — تلات منتجات على الأقل، وكل مقاساتهم:
  `20764` (NF Black × Orange ← `16077`) · `20758` (NF Black × Grey ← `16071`) ·
  `20586` (TH Black × Grey ← `16065`). نطاق الـ IDs القديم (`16065–16077`) بيقول
  إن فيه **نسخة تانية أقدم من نفس المنتجات على ووردبريس** ماسكة الـ SKUs.
  المنتجات دي **مش هتتربط أبدًا** حتى لو الخنق اتحل — بلوكر بيانات مستقل.
  (الرقم اللي ووكومرس بترجّعه في `unique_sku` زي `…/45-1` هو **اقتراحها ببديل**،
  مش الـ SKU بتاعنا — الـ SKU الحقيقي من شوبيفاي هو `NF1 / Black x Orange / 45`.)
  التفاصيل الأصلية: المنتج `20769` رفض مقاس بـ
  `{"code":"product_invalid_sku","message":"Invalid or duplicated SKU.","data":{"resource_id":16077,"unique_sku":"NF1 / Black x Orange / 45-1"}}`
  — يعني **variation رقم `16077`** على ووردبريس ماسكة نفس الـ SKU (غالبًا نسخة
  قديمة/مكرّرة من نفس المنتج). الأداة دلوقتي بتبلّغ بالمقاس ده كـ ⚠ تحذير
  وبتكمّل، لكن **الـ SKU مش هيتزامن** لحد ما حد يفتح `16077` على ووردبريس
  ويحلّ التكرار.
- **المنتجات اللي وقعت في نص التشغيلة دي (`20769` · `20758`) لازم تتعاد** —
  الكتابات على شوبيفاي (الحالة + النجمة + الميتافيلد) اتنفّذت، والمقاسات/التاج
  لأ.

- **⚠️ (v2.6.0) `/wc/v3/products/brands` REST endpoint لسه مش مؤكَّد فعليًا
  ضد `stylebox.online`** — حارس البراند الجديد في `sync_product`
  (`wcSearchBrands()`/`wcFindBrandByName()`) بيفترض إن تاكسونومي `product_brand`
  هو Product Brands الأصلي في ووكومرس (اتضاف رسميًا 9.4+، ومتاح فيه REST
  endpoint بنفس شكل `products/categories`/`products/tags` — `brands:[{id}]`
  في الـ PUT). الافتراض ده مبني على شكل شاشة تحرير المنتج في السكرين المرفق
  مع الطلب ("All Brands" checkboxes + "+ Add New Brand" + رابط
  `edit-tags.php?taxonomy=product_brand`) — **لسه محتاج اختبار حقيقي بعد أول
  نشر** (زي ما اتأكد فلتر `global_unique_id` قبل كده 26-08-2026). لو الـ
  endpoint رجّع 404/401، يبقى المنصة مستخدمة تصنيف مخصّص من پلجن تاني (مش
  الـ Brands الأصلي)، ويحتاج تعديل `wcSearchBrands()`/تحديث المنتج ليستخدم
  endpoint مختلف (مثلاً `wp/v2/product_brand` عبر WP REST العام لو التاكسونومي
  `show_in_rest`).
- تأكيد ما إذا كانت WooCommerce REST secrets (`WC_BASE_URL` / `WC_CONSUMER_KEY` /
  `WC_CONSUMER_SECRET`) لازم تتضاف كأسرار — الكود بيستخدمها فعليًا (`wcGetProduct`
  وغيرها) رغم إن الوصف الأصلي قال "Shopify OAuth بس". لو الأداة رجّعت خطأ ناقص
  متغيرات WC عند أول تشغيل حقيقي، ضيفهم في الداشبورد وحدّث هذا الملف.
  (✅ عمليًا شغّالة فعليًا — الاختبار الحقيقي 26-08-2026 لـ `find_product`
  و`sync_product` نجح، يعني الأسرار دي متظبطة فعلًا في الداشبورد.)
- تسجيل `type` مخصّص لعمليات تحديث السعر في `ecommoda-constants` §7 (بدل
  إعادة استخدام `synced`/`error` مع `extra.priceApplied`) — اختياري، مش إلزامي
  دلوقتي. يستاهل لو محتاجين فلترة/تقرير منفصل لعمليات تحديث السعر في تاب السجل.
- ~~**تغطية حالة "منتج لسه مش مربوط وعنوانه مختلف بين المنصتين"**~~ — ✅
  **اتقفلت في v2.8.0** (27-08-2026) بمسح الـ SKU الاحتياطي
  (`wcScanProductsBySkuPrefix()`) اللي مستقل تمامًا عن العنوان. البند القديم
  كان بيقول إن البحث معتمد بالكامل على تطابق العنوان لتضييق النطاق، وإن
  اختلاف العنوان = "مفيش تطابق" حتى لو المنتج موجود وSKU بتاعه يطابق — وده
  اللي حصل فعلاً 27-08-2026 بسبب **مسافة واحدة زيادة**. راجع قسم v2.8.0 فوق.
- **`_fields` + سقف الـ 3000 منتج في المسح الاحتياطي لسه محتاجين اختبار حقيقي**
  (v2.8.0) — `wcScanProductsBySkuPrefix()` بيفترض إن `_fields` مدعوم على
  `/wc/v3/products` (خاصية عامة في WP REST، المفروض ووكومرس بيرثها). لو مش
  مدعوم، الرد هيرجع كامل بدل الحقول الأربعة — أبطأ وأتقل بس **مش هيكسر**
  المطابقة (بتقرا `sku`/`global_unique_id` من الكائن الكامل عادي). كمان
  `SKU_SCAN_MAX_PAGES = 30` مبني على تقدير إن المتجر تحت 3000 منتج — **العدد
  الحقيقي لسه متأكدش منه**؛ لو عدّاه، الواجهة هتقول "البحث مافحصش كل المنتجات"
  (`scannedAll:false`) وساعتها زوّد السقف.
- **custom WP endpoint للبحث بالـ SKU مباشرة** — لسه يستاهل لو المسح الاحتياطي
  بقى بطيء مع كتر الاستخدام. استعلام واحد `_sku LIKE '<id>-%'` بدل المرور على
  الصفحات، بنفس نمط `ecommoda/v1/variation-stock/{id}` الموجود فعلاً في الستاك
  (محمي بـ `X-EcomModa-Secret`). مش عاجل دلوقتي — المسح الحالي بيحل المشكلة
  بالكامل من غير أي شغل على جهة ووردبريس.

آخر تحديث: 10-09-2026 — 18:40

</div>
