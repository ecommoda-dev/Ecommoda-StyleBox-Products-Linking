# Ecommoda StyleBox Products Linking (`StyleBox-Products-Linking`)

> ⚠️ **أداة جديدة بالكامل من الصفر (مسار N — §9 في ecommoda-tool-migration-playbook)،
> مش نقل أداة موجودة من الداشبورد.** الملف `index.js` كان تاريخيًا
> `shopify-woo-sync-worker` (tool = `shopify_woo_sync`) — اتعمل عليه رينيم كامل
> + إعادة بناء شاملة مقابل `ecommoda-worker-builder` و`woocommerce-sync-helper`
> يوم 25-08-2026 (راجع تعليق §CONSTANTS في أول `index.js` لتفاصيل كل تغيير).
> الكود اتسلّم جاهزًا بالكامل من جلسة Cowork سابقة ونُقل هنا بايت ببايت من غير
> أي لمس لمنطقه.

**بتعمل إيه:** لكل منتج WooCommerce متغيّر مربوط بمنتج Shopify (الربط عبر
`global_unique_id`)، بتزامن الـ SKU/المخزون/الـ metafields لكل Variation، بتحدّث
حالة وعنوان وتاج المنتج على شوبيفاي، وبتنشر المنتج على ووردبريس (`status=publish`).
**مين بيستخدمها:** فريق ربط المنتجات (WooCommerce ↔ Shopify).
**الإصدار:** Worker `v2.7.0` · الواجهة `v2.10.0`.

## نافذة تأكيد المراجعة الإلزامية + إعادة ترتيب "تعديلات تلقائي" + صياغة نتيجة المقاسات — واجهة v2.10.0 (27-08-2026)

بطلب صريح من صاحب الأداة — ثلاث تعديلات UI بحتة، **صفر تغيير في `index.js` أو أي
`action` أو أي payload**.

### 1) نافذة تأكيد إلزامية بعد زرار "راجع صور + كاتيجوريز المنتج قبل الربط"

- الزرار في خطوة ② (نتيجة البحث) لسه بيفتح صفحة المنتج على ووردبريس في تاب
  جديد زي ما هو، لكن دلوقتي كمان بيفتح **نافذة تأكيد إلزامية**
  (`#reviewConfirmOverlay`) فيها بندان checkbox:
  1. "تم مراجعة جميع صور المنتج"
  2. "تم اضافة كاتيجوري فئة الشوز + كاتيجوري البراند"
- زرار "✅ تأكيد المراجعة" جوه النافذة **معطّل** لحد ما الاتنين يتأكدوا
  (`updateReviewConfirmBtn()`). الضغط عليه بينادي `markProductReviewed()` نفسها
  (نفس المنطق اللي كان بيتنفذ مباشرة من onclick الزرار قبل كده) — هي اللي
  بتفتح خطوة ③ لأول مرة. زرار "إلغاء" أو الضغط برّه النافذة بيقفلها من غير
  ما يفتح خطوة ③.
- راجع `openReviewConfirmModal()`/`closeReviewConfirmModal()`/
  `toggleReviewCheck()`/`confirmProductReviewed()` في `index.html`.

### 2) صندوق "تعديلات أخرى تحدث تلقائي عند الربط" — نقل لآخر الخطوة + بند جديد

- الصندوق (`.opt-block` عرض فقط، v2.9.0) كان أول حاجة في خطوة ③ فوق الأسئلة
  التلاتة — دلوقتي بقى **آخر حاجة** في الخطوة، بعد سؤال "تحديث السعر" مباشرة
  وقبل زرار "ابدأ ربط المنتج".
- بند جديد اتضاف تحت "على Stylebox.online": **"التأكد من ضبط رابط المنتج
  (URL/Slug) ليطابق عنوانه"** — توثيق لسلوك `slugify()`/`slugFixed` الموجود
  فعلاً في `index.js` من v2.6.0 (كان موصوف في CLAUDE.md بس مش ظاهر في نص
  الصندوق نفسه في الواجهة).

### 3) خطوة ④ "النتيجة" — عنوان جدول المقاسات + مكان جدول فرق السعر

- عنوان جدول الـ Variations بقى **"📦 المقاسات: ✓ تمت (N) | ⚠ تحذير (N) |
  تخطي (N)"** بدل "📦 الـ Variations — ✓ N تمت | ⚠ N تحذير | تخطي N — من أصل
  N" (اتشالت "من أصل N" من الصيغة).
- سطر "فرق السعر المستخدم" (لو `priceApplied`) اتنقل من فوق جدول الـ
  Variations لجدول `<table class="variant-table">` مستقل واضح بصريًا (عنوان +
  صف قيمة) **تحت** الجدول مباشرة.

## حارس "اتربط قبل كده" + مراجعة إلزامية + إعادة ترتيب النتيجة — Worker v2.7.0 · واجهة v2.9.0 (27-08-2026)

بطلب صريح من صاحب الأداة — خمس تعديلات، بند واحد بس فيه منطق فعلي جديد في
الـ Worker (البند 1)، الباقي UI بحت في `index.html`.

### 1) حارس "اتربط قبل كده" — أول حاجة في `find_product`، على مستوى الـ Worker

- قبل أي بحث في ووكومرس، الـ Worker بيتحقق من metafield
  `custom.wordpress_id` على منتج شوبيفاي المطلوب (نفس الـ metafield اللي
  `syncProduct` بيكتبها في خطوة 2 من ترتيب التنفيذ). لو فيها قيمة (مش
  فاضية)، يبقى المنتج ده اتربط قبل كده.
- **الرد في الحالة دي:** `{ok:true, alreadyLinked:true, wordpressId,
  productTitle}` — الواجهة بتعرض رسالة **"هذا المنتج تم ربطه من قبل"** وما
  بتفتحش خطوة الربط خالص (خطوة ③ بتفضل مخفية).
- لو المنتج لسه مش مربوط (`alreadyLinked:false`)، البحث بيكمل زي ما كان
  بالظبط (فلتر GTIN → بحث بعنوان شوبيفاي لتضييق النطاق → تأكيد قاطع بالـ
  SKU/GTIN — راجع `findWcProductByShopifyId()`).
- راجع `checkShopifyAlreadyLinked()`/`PRODUCT_WPID_CHECK_QUERY` في
  `index.js` §FIND.

### 2) زرار مراجعة إلزامي في خطوة ② "نتيجة البحث" — واجهة فقط

- اتشال صف SKU وصف "اتلقى عن طريق" من نتيجة البحث.
- زرار "عرض المنتج على وردبريس للمراجعة" اتغيّر اسمه لـ**"راجع صور +
  كاتيجوريز المنتج قبل الربط"** وبقى **إلزامي الضغط عليه**: خطوة ③ (الربط)
  ما بتظهرش خالص إلا بعد الضغط عليه — `markProductReviewed()` هي اللي
  بتفتحها. `runSync()` كمان بيرفض التشغيل لو `productReviewed !== true`
  (دفاع مزدوج، زي أي حارس تاني في الأداة).

### 3) نافذة "البراند غير موجود" — حذف جملة

- اتشالت جملة "(Vendor على شوبيفاي)" من نص رسالة النافذة — النص بقى "لا
  يوجد براند بنفس اسم X على ووردبريس..." من غير الجملة المعترضة دي.

### 4) قسم "يحصل دائمًا بدون خيار" — إعادة تصميم بنفس شكل الأسئلة التانية

- بقى صندوق `.opt-block` بنفس تصميم باقي أسئلة خطوة ③ بالظبط (عرض فقط،
  بدون أي اختيار)، بعنوان جديد **"تعديلات أخرى تحدث تلقائي عند الربط:"**
  (بدل "يحصل دائمًا في كل تشغيلة — بدون أي اختيار:")، مقسّم لقسمين:
  - **على Stylebox.online:** تغيير حالة المنتج إلى Published، إضافة
    البراند مثل الـ Vendor على شوبيفاي، مزامنة كمية المخزون لكل مقاس/SKU.
  - **وعلى Ecommoda.com:** إضافة Wordpress IDs على المنتج وعلى كل مقاس،
    إضافة تاج `stylebox` على شوبيفاي.

### 5) خطوة ④ "النتيجة" — إعادة ترتيب + تعديل نص

- بانر "✅ تم الربط بنجاح" وزرار "🆕 بدء منتج جديد" اتنقلوا لآخر الخطوة —
  بعد تفاصيل بيانات المنتج والـ Variations، مش قبلها.
- زرار "بدء منتج جديد" بقى زرار كامل عرض الصفحة لوحده (اتشال النص
  التوضيحي اللي كان جنبه في `.new-product-bar` القديمة).
- في سطر ملخّص البانر: كلمة "Variation" اتغيّرت لـ**"مقاس"**، وكلمة "تمت"
  اتغيّرت لـ**"تمت المزامنة"**.

## حارس البراند + تصحيح الـ Slug — v2.6.0 (27-08-2026)

بطلب صريح من صاحب الأداة — تعديلان جديدان في منطق `sync_product` نفسه (على
عكس v2.6.0/v2.7.0 اللي فوق، اللي كانت UI بحت). **الاتنين في الـ Worker
(`index.js`) — الواجهة (v2.8.0) بتعرض النتيجة/الخطأ بس، صفر منطق جديد
فيها.**

### 1) حارس براند إلزامي — قبل أي كتابة في `syncProduct`

- الـ Vendor على شوبيفاي (حقل `vendor` على المنتج) لازم يكون له براند بنفس
  الاسم بالظبط (مطابقة حرفية case-insensitive بعد trim) على تاكسونومي
  `product_brand` في ووردبريس — نفس التاكسونومي اللي شاشة تحرير المنتج على
  ووردبريس بتاعته فيها "All Brands" checkboxes + "+ Add New Brand"
  (`/wp-admin/edit-tags.php?taxonomy=product_brand&post_type=product`).
- **لو مفيش براند مطابق: الربط بالكامل بيتوقف من غير أي كتابة على أي منصة**
  (لا شوبيفاي ولا ووكومرس اتلمسوا) — رد مُبنيَن من الـ Worker
  `{ok:false, code:'brand_missing', vendor, addBrandUrl}` بـ HTTP 409. الواجهة
  بتعرض نافذة خطأ فيها زرار **"➕ إضافة البراند على StyleBox"** بيفتح صفحة
  إضافة البراند مباشرة في تاب جديد.
- **لو موجود:** بيتربط بالمنتج تلقائيًا (`brands:[{id}]`) في نفس نداء
  `status=publish` (خطوة 3 تحت) — مفيش نداء PUT إضافي.
- الـ Vendor فاضي على شوبيفاي = الحارس بيتخطّى تمامًا (مفيش حاجة تتطابق
  أصلاً) — مش اعتباره "براند موجود".
- راجع `BrandNotFoundError`/`wcFindBrandByName()`/`wcSearchBrands()` في
  `index.js`.

⚠️ **لسه مش مؤكَّد فعليًا** إن `/wc/v3/products/brands` REST endpoint شغّال
على `stylebox.online` (زي فلتر `global_unique_id` قبل ما يتأكد فعليًا
26-08-2026) — الافتراض إنه تاكسونومي البراندات الأصلي في ووكومرس 9.4+ بناءً
على شكل الشاشة في السكرين المرفق مع الطلب (نفس تصميم Categories/Tags +
"+ Add New Brand"). راجع "مسائل مفتوحة" تحت.

### 2) تصحيح الـ Slug — إلزامي بدون خيار

- عند كل `sync_product`، لو الـ `slug` بتاع منتج ووردبريس مش مطابق
  للـ slug المتوقع من عنوانه (`slugify(wooProduct.name)` — أحرف صغيرة،
  استبدال أي حرف مش a-z0-9 بشرطة، دمج الشرطات المتتالية، قص الشرطات من
  الطرفين)، بيتصلّح تلقائيًا في نفس نداء `status=publish` (خطوة 3 في ترتيب
  التنفيذ تحت) — مفيش نداء PUT إضافي.
- مثال: `The North Face Vectiv Enduris 3 - in Beige` →
  `the-north-face-vectiv-enduris-3-in-beige`.
- مفيش خيار لتعطيل الخطوة دي — زي نشر ووردبريس (`status=publish`) بالظبط.
- العنوان المصدر هو عنوان **ووكومرس** (`wooProduct.name`) — مش عنوان شوبيفاي
  (اللي ممكن يكون فيه "⭐ " مُضافة من الأداة نفسها).
- راجع `slugify()`/`slugFixed` في `index.js`.

## تعديلات UI إضافية — v2.7.0 (27-08-2026)

جولة تعديلات ثانية بطلب صاحب الأداة على نفس إعادة التصميم (v2.6.0 تحت).
**برضه تعديل UI بحت — صفر تغيير على `index.js` أو أي `action` أو أي payload.**

- **"نتيجة البحث" بقت خطوة ② مستقلة لوحدها** (كانت جوه خطوة البحث نفسها) —
  الخطوات بقت أربعة: ① بحث → ② نتيجة البحث → ③ الربط وتحديث السعر → ④ النتيجة.
- **زرار "🔗 عرض المنتج على وردبريس للمراجعة"** وزرار الربط النهائي بقوا
  `.btn-block` — زرار كبير كامل عرض الصفحة (زي زرار "تحديث الكل" في أدوات
  تانية) بدل زرار صغير. زرار الربط اتغيّر اسمه من "▶ تشغيل الربط" لـ
  **"▶ ابدأ ربط المنتج بين الموقعين الآن"** ولونه بقى أخضر (`.success`).
- **أسئلة الربط التلاتة (الحالة/النجمة/السعر) بقت صناديق `.opt-block`
  مستقلة بإطار**، عنوان السؤال والاختيارات في نفس الصف (مش فوق بعض) —
  بدل ما تكونوا مجرد فقرات متتالية.
- **شيبس "ⓘ تفاصيل" (الحالة/النجمة) وشيبس "ⓘ إزاي بيتحسب السعر؟" اتشالوا
  بالكامل** بطلب صريح من صاحب الأداة.
- **جملة "بيحصل دايمًا بدون خيار" بقت صندوق ثابت `.always-box` ظاهر
  دايمًا** (🔒) — مش شيبس قابلة للطي زي باقي الأسئلة، عشان توضّح إنه إجراء
  إجباري مش سؤال محتاج إجابة من المستخدم.
- **خطوة ① بقت تقبل رابط منتج شوبيفاي بالكامل** (من صفحة المنتج على أدمن
  شوبيفاي) مش الرقم الصريح بس — `extractShopifyProductId()` بيستخرج الرقم
  من الرابط على مستوى الواجهة. القيمة اللي بتتبعت لـ `find_product` لسه
  رقم صريح زي الأول، صفر تغيير في العقد مع الـ Worker.
- **إصلاح تنبيه "⚠️ نسخة الـ Worker مختلفة"** — كان بيقارن نسخة الواجهة
  (`TOOL_VERSION`) مباشرة بنسخة الـ Worker، فكان بيطلع تحذير زائف كل مرة
  تتعدّل فيها الواجهة بس (زي جولة v2.6.0/v2.7.0 دي) من غير أي لمس للـ
  Worker — رغم إن الاتنين نسختين مستقلتين زي ما موثّق فوق. دلوقتي بيقارن
  بثابت جديد `EXPECTED_WORKER_VERSION = 'v2.5.0'` (نسخة الـ Worker اللي
  الواجهة اتبنت عليها فعليًا) — الثابت ده بيتحدّث يدويًا **بس لو `index.js`
  نفسه اتغيّر فعلاً وترقّى على Cloudflare**، مش مع كل تعديل واجهة.

## إعادة تصميم الواجهة (UI فقط) — v2.6.0 (27-08-2026)

بطلب صريح من صاحب الأداة — الشكل القديم كان "بدائي" وخطوطه صغيرة ومليان
شروحات ظاهرة دايمًا. **تعديل UI بحت على `index.html`، صفر تغيير على
`index.js` أو أي `action` أو أي payload.**

- خطوات "ربط منتج" بقت بطاقات مرقّمة (① بحث → ② ربط + سعر → ③ نتيجة) —
  كل خطوة بتتطوى تلقائيًا وهي بتتنقل للتالية (زي طلب المستخدم: "حاجات
  المفروض تكون Collapsed لما تنتقل من مرحلة لمرحلة")، وتفتح تاني بالضغط
  على عنوانها.
- خط ومسافات أكبر في كل مكان، أزرار أكبر وأوضح (`.btn-primary` بقى بارز
  بظل وhover)، وشروحات الأسئلة الطويلة بقت اختيارية خلف زرار "ⓘ تفاصيل"
  بدل ما تكون ظاهرة دايمًا (كانت المشكلة اللي المستخدم سمّاها "مليانة
  Placeholders بدون داعي").
- شاشة النتيجة (خطوة ③) اتعاد تصميمها بالكامل: بانر حالة كبير ملوّن
  (أخضر/أصفر/أحمر) بدل عرض النتيجة كبطاقة عادية زي باقي الأداة.
- زرار جديد **"🆕 بدء منتج جديد"** أعلى خطوة النتيجة — بيصفّر كل حاجة
  (البحث + الخيارات + النتيجة) ويرجّع الأداة لخطوة ① من غير Refresh —
  ده كان ناقص تمامًا قبل كده.
- راجع `§TOOL-JS::steps` في `index.html` (`toggleStep`/`setStepCollapsed`/
  `toggleHint`/`startNewProduct`) والقسم الأول في About modal للتفاصيل.

## ترتيب التنفيذ في `sync_product` (مقصود — متغيّرهوش من غير سبب)

```
1. قراءة منتج ووكومرس + الـ Variations + منتج شوبيفاي
1.5. ⚠️ حارس إلزامي (v2.6.0) : لازم براند على ووردبريس بنفس اسم الـ Vendor
     على شوبيفاي — وإلا الربط بالكامل يتوقف من غير أي كتابة (BrandNotFoundError)
2. Shopify product-level : status (حسب الخيار) + ⭐ (حسب الخيار) + metafield wordpress_id
3. WooCommerce product-level : status='publish' (دايمًا، بدون خيار) + meta _shopify_product_id
   + slug fix (v2.6.0، دايمًا بدون خيار) + ربط الـ Brand (v2.6.0، لو الخطوة 1.5 لقت تطابق)
4. لكل Variation : SKU/مخزون/meta على ووكومرس + wordpress_variation_id على شوبيفاي
5. tagsAdd("stylebox") فورًا ← آخر خطوة على الإطلاق (بدون أي انتظار)
```

الخطوة 1.5 بتوقف العملية بالكامل لو فشلت (زي فشل `no global_unique_id`) —
مش معزولة في `try/catch` مستقل زي 2/3/5. الخطوات 2 و3 و5 كل واحدة معزولة في
`try/catch` لوحدها — فشل واحدة مايوقفش التانية.

> تعديل 26-08-2026 (v2.2.1): الانتظار كان 5 ثواني، اتغيّر لـ 10 ثواني بناءً على طلب
> صاحب الأداة. راجع `TAG_DELAY_MS` في `index.js` (وقتها).
>
> ⚠️ تعديل 26-08-2026 (v2.4.0): الانتظار (`TAG_DELAY_MS`) **اتلغى بالكامل** —
> بطلب صريح من صاحب الأداة نفس اليوم. `tagsAdd` لسه بيتنفّذ **آخر خطوة على
> الإطلاق** زي ما هو (الترتيب نفسه ما اتغيّرش) — الملغي هو الانتظار العشر
> ثواني قبله بس. الثابت `TAG_DELAY_MS` اتشال تمامًا من `index.js`.

### GTIN fallback من الـ SKU (v2.2.0 — 26-08-2026)

في الخطوة 1، لو `global_unique_id` (خانة GTIN) فاضية على منتج ووكومرس، الـ
Worker بيدوّر على رقم شوبيفاي في **بداية** الـ `SKU` (نمط `رقم-slug`، زي
`10468835819842-skechers-slip-ins-…`، حد أدنى 6 أرقام). لو لقاه:
يكتب الرقم في `global_unique_id`، ويمسحه من الـ SKU (يفضل الباقي بس كـ SKU
جديد)، ويكمّل بقية `sync_product` عادي بنفس الرقم. لو مفيش رقم في الـ SKU
برضه، الفشل زي ما كان (`no global_unique_id ... — skipping`).
راجع `extractGtinFromSku()` في `index.js`.

## أكشن `find_product` (v2.4.0 — خطوة 1 في الواجهة، بحث بالعنوان اتضاف v2.5.0، حارس "اتربط قبل كده" اتضاف v2.7.0)

الواجهة اتعاد تصميمها بالكامل (v2.4.0) على خطوتين: الخطوة 1 بحث، الخطوة 2 ربط
+ تحديث سعر. `find_product` هو أكشن الخطوة 1 — **قراءة بس، مفيش كتابة ومفيش D1
log** (زي `diag`/`get_config`). مربع الإدخال بيستجيب لـ Enter كمان مش زرار
البحث بالماوس بس (اتصلّح v2.5.0).

```
GET ?action=find_product&shopify_product_id=10468878713154
```

### 0) حارس "اتربط قبل كده" — أول حاجة بتتنفّذ (v2.7.0)

قبل أي بحث في ووكومرس، الـ Worker بيتحقق من metafield `custom.wordpress_id`
على منتج شوبيفاي المطلوب. لو فيها قيمة (مش فاضية)، يبقى المنتج ده اتربط قبل
كده — الرد بيرجع `{ok:true, alreadyLinked:true, wordpressId, productTitle}`
من غير ما يكمّل بحث WC خالص. الواجهة بتعرض "هذا المنتج تم ربطه من قبل" ووقف
— خطوة الربط ما بتظهرش. لو `alreadyLinked:false`، البحث بيكمل زي القسم تحت
بالظبط. راجع `checkShopifyAlreadyLinked()`/`PRODUCT_WPID_CHECK_QUERY`.

- الموظف بيدخل **رقم منتج شوبيفاي الرقمي** (من رابط المنتج على أدمن شوبيفاي،
  زي `.../products/10468878713154`) — مش رقم منتج ووكومرس زي قبل كده.
- الـ Worker بيدوّر على منتج ووكومرس اللي يطابقه: إما `global_unique_id`
  (GTIN) بيساوي الرقم، أو الرقم في **بداية** الـ SKU (نفس ريجيكس
  `extractGtinFromSku`، لكن بالعكس — هنا الرقم معروف من الأول وبندوّر بيه على
  المنتج).
- **محاولتان لتجميع مرشّحين، لكن القبول النهائي بيعتمد على GTIN/SKU بس أبدًا**:
  1. فلتر `global_unique_id` مباشر على `/wc/v3/products` — رسمي في ووكومرس
     9.2+ (نفس الحقل الظاهر في خانة "GTIN, UPC, EAN, or ISBN" في محرر
     المنتج). ✅ **مؤكَّد شغّال فعليًا على `stylebox.online`** (تجربة حقيقية
     26-08-2026 — Ahmed دخل الرقم يدويًا في خانة GTIN لمنتج والبحث لقاه فورًا).
  2. **(v2.5.0)** بحث بعنوان المنتج على شوبيفاي (`search=`) — الـ Worker بيجيب
     عنوان المنتج من شوبيفاي بالـ GraphQL الأول، بعدين يبحث بيه في ووكومرس.
     ⚠️ **العنوان هنا لتضييق نطاق البحث بس، مش تأكيد نهائي** — ممكن منتجين
     يتشابهوا في العنوان، أو العنوان يختلف جزئيًا بين المنصتين. التأكيد
     النهائي القاطع لسه GTIN أو SKU بس (نفس ريجيكس `extractGtinFromSku`)،
     أبدًا مش بالعنوان.
  > ⚠️ **المحاولة القديمة (v2.4.0) كانت بحث نصي بالرقم نفسه (`search=idStr`) —
  > اتشالت v2.5.0.** تجربة حقيقية (26-08-2026) أثبتت إن ووكومرس هنا **مبيدوّرش
  > بيها على الـ SKU خالص** — رجّعت صفر مرشّحين حتى لمنتج SKU بتاعه بيبدأ
  > حرفيًا بالرقم المطلوب (`10468878713154-the-north-face-…`). لو حد شاف
  > الكود القديم بيستخدم `search=` بالرقم، ده اللي كان بيحصل.
- لو لقى تطابق، بيرجّع `wp_product_id` + `productName` + `sku` + `matchedBy`
  (`gtin`/`sku`) + `wpEditUrl` (رابط صفحة تعديل المنتج جاهز، مبني من
  `WC_BASE_URL` — مفيش سر جديد مطلوب). الواجهة بتعرض زرار "🔗 عرض المنتج على
  وردبريس للمراجعة" بيه، وبتفتح الخطوة 2 (خيارات الربط + تحديث السعر)
  بالـ `wp_product_id` ده جاهز — الموظف مبقاش بيكتبه يدوي خالص.
- لو مفيش تطابق، بيرجّع `{ found: false, scanned: N }` (مش خطأ — حالة متوقعة).
  ⚠️ **دلوقتي (بعد شيل fallback الرقم) دي بتحصل فعليًا لأي منتج لسه مش مربوط**
  **وعنوانه على ووردبريس مختلف بما يكفي عن عنوانه على شوبيفاي** — لو حصلت،
  الحل اليدوي البديل: افتح المنتج على ووردبريس وحط الرقم في خانة GTIN بنفسك،
  والبحث هيلاقيه فورًا عن طريق فلتر GTIN (المحاولة 1).

راجع `findWcProductByShopifyId()`/`wcSearchProducts()` في `index.js`.

## خيارات `sync_product` (v2.1.0 — فرق السعر اتضاف v2.3.0 كأكشن منفصل، واندمج هنا v2.5.0)

| البراميتر | القيم | الافتراضي |
|---|---|---|
| `shopify_status` | `ACTIVE` · `DRAFT` · `KEEP` (ما تبعتش `status` خالص) | `DRAFT` |
| `add_star` | `true` · `false` | `true` |
| `price_difference` | رقم (يقبل سالب) أو مفيش خالص | مفيش — تحديث السعر يتخطّى تمامًا |

> `skip_draft` / `skip_star` القديمين لسه مقبولين كـ fallback في الـ Worker
> (`skip_draft:true` ≡ `KEEP`، `skip_star:true` ≡ `add_star:false`) عشان أي واجهة
> قديمة في كاش المتصفح ماتكسرش. الواجهة الحالية مبتبعتهمش خالص.

### فرق السعر (`price_difference`) — v2.5.0: اندمجت جوه `sync_product`

> ⚠️ **كان أكشن منفصل تمامًا `update_price` (v2.3.0، 26-08-2026) — اتشال
> بالكامل واندمج جوه `sync_product` نفسها v2.5.0 (نفس اليوم) بطلب صريح من
> صاحب الأداة: "متكونش خطوة منفصلة بعد الربط، المفروض تكون جزء من عملية
> الربط نفسها، زي إضافة النجمة للعنوان مثلاً."** لو لقيت أي إشارة لأكشن
> `update_price` في كود قديم أو نسخة محفوظة، ده بقى تاريخي بس.

- `price_difference` **اختياري تمامًا** — لو مبعوتش/فاضي، خطوة تحديث السعر
  بتتخطّى تمامًا (سلوك `sync_product` القديم قبل v2.3.0 زي ما هو، بدون أي أثر).
  مستقل تمامًا عن `shopify_status`/`add_star` — قيمة واحد مالهاش أي أثر
  على التانيين، بالظبط زي علاقة الحالة بالنجمة.
- بييجي من حقل في الواجهة مباشرة، **مش** `env var` زي `PRICE_DIFFERENCE` في
  أداة "مزامنة أسعار Stylebox". القيمة بتتحفظ في `localStorage`
  (`stylebox_link_price_difference`) كآخر قيمة استُخدمت **للراحة بس** — مفيش
  قيمة افتراضية مبيّتة في الكود، ومفيش إرسال تلقائي من غير ما المستخدم يضغط
  "تشغيل الربط".
- **الكتابة بتحصل في نفس نداء `wcUpdateVariation` بتاع SKU/المخزون** (خطوة 4
  في ترتيب التنفيذ فوق) — مش نداء إضافي، ومش خطوة منفصلة زمنيًا. `regular_price`/
  `sale_price` بيتحطّوا في نفس الـ payload.
- **المطابقة بالمقاس** — نفس آلية باقي `sync_product` (`findWcSize`/
  `findShopifyVariantBySize`)، **مش** `wordpress_variation_id`/GTIN triple-check
  بتاع أداة "مزامنة أسعار Stylebox".
- **معادلة الحساب** (نفس معادلة `stylebox-price-sync-worker` حرفيًا —
  `computeVariantPrices()`): لو فيه خصم فعلي على شوبيفاي
  (`compareAtPrice > price`) → `regular_price = compare_at + diff` و
  `sale_price = price + diff`. وإلا → `regular_price = price + diff` و
  `sale_price` بيتفضّى (بيمسح أي خصم قديم على ووكومرس).
- **مفيش GTIN-from-SKU fallback خاص بالسعر** — لو المنتج لسه مش مربوط
  (`global_unique_id` فاضي)، العملية كلها بترفض زي أي `sync_product` تاني
  (الـ fallback العادي بتاع `syncProduct` بيتطبّق زي ما هو، مفيش استثناء).
- **D1:** بيستخدم نفس `type = 'synced'`/`'error'` الأصليين لكل variation —
  **مفيش `type` جديد ولا `extra.operation` منفصل** (كان `extra.operation =
  'price_update'` في v2.3.0 — اتشال). المُميِّز دلوقتي `extra.priceApplied = true`
  في صف الـ variation لو الخطوة دي اتنفذت.

## الروابط

```
الواجهة    : https://ecommoda-dev.github.io/StyleBox-Products-Linking/
الـ Worker : https://stylebox-products-linking-worker.ecommoda-dev.workers.dev
اسم الـ Worker في الداشبورد: stylebox-products-linking-worker     ← لازم يطابق name في wrangler.toml
```

## الـ Endpoints

| `?action=` | بيعمل إيه |
|---|---|
| `find_product` | البحث بمعرف شوبيفاي (v2.4.0، بحث بالعنوان اتضاف v2.5.0) — خطوة 1، GET، قراءة بس (`shopify_product_id` مطلوب). ⚠️ (v2.7.0) أول حاجة بيتحقق هل المنتج اتربط قبل كده (`custom.wordpress_id` مش فاضي) ويرجع `{alreadyLinked:true, wordpressId}` لو آه — راجع القسم فوق |
| `sync_product` | العملية الأساسية — ربط/مزامنة منتج واحد (POST، `wp_product_id` مطلوب، + `shopify_status`/`add_star`/`price_difference` اختياريين — راجع القسم فوق). ⚠️ (v2.6.0) ممكن يرجع HTTP 409 `{code:'brand_missing', vendor, addBrandUrl}` لو مفيش براند مطابق على ووردبريس |
| `check_employee` / `register_pin` / `verify_employee` / `log_logout` / `get_employees` | Universal D1 Auth |
| `diag` | تشخيص env/Shopify/WC/D1 |
| `get_config` | نسخة الـ Worker |
| `get_logs` / `get_logs_count` / `get_logs_export` | سجل العمليات |

## D1

```
tool  : stylebox_products_linking
type  : product_meta_synced · synced · error · login · logout
```

> لو القيم دي مش في جدول D1 في `ecommoda-constants` §7 → ضيفها هناك الأول.

> ⚠️ **تحديث السعر (`price_difference` جوه `sync_product`) بيستخدم نفس
> `type = 'synced'`/`'error'` فوق — مفيش `type` جديد.** المُميِّز
> `extra.priceApplied = true` جوه صف الـ variation (كان `extra.operation =
> 'price_update'` وقت ما كان أكشن `update_price` منفصل في v2.3.0 — اتشال
> مع دمج الميزة v2.5.0). قرار مقصود (مش سهو): الجلسة اللي بنت الميزة دي
> معندهاش وصول لريبو `ecommoda-constants` عشان تسجّل `type` جديد فيه قبل أول
> استخدام (Rule 7). لو محتاج فلترة منفصلة لعمليات تحديث السعر في تاب السجل
> مستقبلًا، سجّل `type` مخصّص (مثلاً `price_synced`) هناك **الأول** قبل أي
> تعديل تاني.

## المضبوط فعليًا في الداشبورد

> اللي **متظبط بالفعل** — مش اللي المفروض يكون.

```
Bindings : DB → ecommoda-dev-logs
Secrets  : WORKER_SECRET · CLIENT_ID · CLIENT_SECRET   ← Shopify OAuth بس
           (الأداة دي مبتكلّمش WooCommerce REST مباشرة عبر أسرار WC — راجع index.js
            لو WC_BASE_URL/WC_CONSUMER_KEY/WC_CONSUMER_SECRET اتفعّلوا فعليًا)
Vars     : SHOP_DOMAIN   ← من [vars] في wrangler.toml. مفيش LOCATION_ID (مش أداة مخزون)
Build watch paths : * الافتراضي
```

## CORS

`ALLOWED_ORIGINS` صارمة (`https://ecommoda-dev.github.io` فقط) — Option B، لأن
الأداة **كتابة**: بتغيّر حالة/عنوان/تاج المنتج على شوبيفاي ومخزون/SKU على WooCommerce.

## خط الأساس بعد النقل

> الأرقام اللي الأداة رجّعتها بعد ما اتأكدنا إنها شغالة — مرجع لأي شك بعد كده.

```
لسه محتاج تسجيل بعد أول اختبار حقيقي بعد الربط في Cloudflare.
```

## فخاخ الأداة دي

- الأداة دي **manual-only بالتصميم** — مفيش `scheduled()` ومفيش `sync_all`. أي
  إضافة Cron أو دفعة تشغيل جماعي = مخالفة مقصودة، راجع تعليق §CONSTANTS في
  `index.js` قبل ما تضيفها.
- `shopify_status` و`add_star` خياران **مستقلّان تمامًا** عن بعض — قيمة واحد
  مالهاش أي أثر على التاني. لا تدمجهم في خيار واحد.
- الـ Tag `stylebox` **لازم يفضل آخر خطوة** في `syncProduct` — ده طلب صريح من
  صاحب الأداة مش تفصيلة تنفيذية. أي نقل ليه لأول العملية = مخالفة مقصودة.
  ⚠️ **الانتظار العشر ثواني اللي كان قبله (`TAG_DELAY_MS`) اتلغى بالكامل
  v2.4.0** — القاعدة القديمة ("الحذف للانتظار = مخالفة") اتقلبت: صاحب الأداة
  نفسه طلب الإلغاء 26-08-2026. لو رجّعت الانتظار من غير طلب صريح جديد، ده
  اللي بقى مخالفة دلوقتي.
- الواجهة (v2.4.0) بقت **خطوتين إلزاميتين**: البحث بمعرف شوبيفاي (`find_product`)
  الأول، وبعده الربط/تحديث السعر — مفيش إدخال يدوي لـ WooCommerce Product ID
  خالص، لازم يجي من نتيجة البحث. أي رجوع لإدخال يدوي بدون البحث = مخالفة
  مقصودة لهذا التصميم.
- نشر المنتج على ووردبريس (`status=publish`) **بدون خيار** — بيحصل في كل تشغيلة.
  التأكيد بيتقرا من رد ووكومرس نفسه (`status === 'publish'`) مش من HTTP 200.
- كل عملية `sync_product` لازم تكون من موظف مسجّل دخول (Universal D1 Auth) —
  مفيش استثناء "تشغيل يدوي بدون تسجيل دخول" هنا رغم إن الأداة manual-only.
- **(v2.6.0) حارس البراند بيوقف العملية بالكامل** — لو الـ Vendor على شوبيفاي
  معندوش براند مطابق على ووردبريس، الحارس ده **قبل** أي كتابة (حتى قبل
  الخطوة 2 Shopify product-level) — مش زي فشل باقي البلوكات (2/3/5) اللي كل
  واحد معزول في `try/catch` مايوقفش التانية. أي طلب "خلّي الربط يكمل حتى لو
  البراند مفقود" = رجوع عن تصميم مقصود صريح — يحتاج طلب جديد.
- **(v2.6.0) تصحيح الـ Slug بدون خيار** — بيحصل في كل تشغيلة زي
  `status=publish` بالظبط، مفيش parameter لتعطيله. العنوان المصدر هو عنوان
  ووكومرس (`wooProduct.name`) — مش عنوان شوبيفاي (اللي ممكن يحمل "⭐ ").
- تحديث السعر (`price_difference`) **جزء من `sync_product` نفسها من v2.5.0** —
  مفيش أكشن `update_price` منفصل خالص (اتشال بالكامل، كان موجود في v2.3.0
  بس). أي طلب لفصله تاني عن الربط = رجوع لتصميم قديم اتشال عمدًا بطلب صاحب
  الأداة — يحتاج طلب صريح جديد.
- فرق السعر (`price_difference`) بيتحفظ في `localStorage` **للراحة بس** (آخر
  قيمة استُخدمت) — مفيش قيمة افتراضية صامتة في الكود، ولازم المستخدم يدخلها
  ويضغط "تشغيل الربط" في كل مرة. لو حد غيّر القيمة في الحقل ونسي، مفيش هامش
  "افتراضي" بيتطبّق بدل منها زي `getPriceDiff()` في أداة الأسعار التانية.
  حقل فاضي = تخطّي تحديث السعر تمامًا، مش صفر.
- **(v2.7.0) حارس "اتربط قبل كده" في `find_product` بيوقف البحث بالكامل** —
  لو `custom.wordpress_id` على شوبيفاي مش فاضي، الـ Worker بيرجّع
  `alreadyLinked:true` من غير ما يكمّل بحث ووكومرس خالص (مش نفس منطق GTIN/SKU
  تحت). أي طلب "كمّل البحث حتى لو المنتج مربوط" = رجوع عن تصميم مقصود صريح.
- **(v2.9.0) زرار "راجع صور + كاتيجوريز المنتج قبل الربط" إلزامي** — خطوة ③
  (الربط) ما بتظهرش في الواجهة إلا بعد الضغط عليه (`markProductReviewed()`)،
  و`runSync()` كمان بيرفض التشغيل لو `productReviewed !== true`. أي طلب
  "خلّي خطوة الربط تظهر تلقائي زي قبل v2.9.0" = رجوع عن تصميم مقصود.
- **(v2.10.0) نافذة تأكيد المراجعة (بندين checkbox) إلزامية قبل ما
  `markProductReviewed()` تتنفّذ** — الزرار بقى بينادي `openReviewConfirmModal()`
  مش `markProductReviewed()` مباشرة؛ زرار "تأكيد المراجعة" جوه النافذة معطّل
  لحد ما الاتنين البندين يتأكدوا. أي طلب "رجّع الزرار يفتح خطوة ③ على طول
  من غير نافذة" = رجوع عن تصميم مقصود.

## استرجاع النسخ القديمة

> ده بديل الـ tags — دفع الـ tags ممنوع من جلسات Claude Code السحابية.

```
v2.0.0 → commit 3234fb1 (آخر commit قبل تعديلات 26-08-2026)
git checkout 3234fb1 -- index.js index.html
```

## بصمة المهارات

> الصيغة والقواعد والمهارات اللي بتدخل الجدول → `ecommoda-skill-versioning`
> Step 4. مهارة مالهاش رقم إصدار مابتدخلش الجدول.

| المهارة | الإصدار وقت آخر تعديل |
|---|---|
| ecommoda-worker-builder | v1.0.0 |
| ecommoda-html-builder | v1.0.0 |
| woocommerce-sync-helper | v1.0.0 |

آخر مطابقة: 27-08-2026 · `index.js` v2.7.0 (حارس "اتربط قبل كده") · `index.html` v2.10.0 (نافذة تأكيد المراجعة + إعادة ترتيب "تعديلات تلقائي" + صياغة نتيجة المقاسات)
🔴 معلّقة: — لا شيء

> سطر **🔴 معلّقة** = أي بند كاسر **معروف ومتقرر تأجيله**، بسببه.
> `— لا شيء` معناها مفيش. **بند 🔴 متأجل من غير ما يتكتب هنا = بند ضايع** —
> مفيش ملف تاني في المشروع بيتتبّعه.

## مسائل مفتوحة

- **⚠️ (v2.6.0) `/wc/v3/products/brands` REST endpoint لسه مش مؤكَّد فعليًا
  ضد `stylebox.online`** — حارس البراند الجديد في `sync_product`
  (`wcSearchBrands()`/`wcFindBrandByName()`) بيفترض إن تاكسونومي `product_brand`
  هو Product Brands الأصلي في ووكومرس (اتضاف رسميًا 9.4+، ومتاح فيه REST
  endpoint بنفس شكل `products/categories`/`products/tags` — `brands:[{id}]`
  في الـ PUT). الافتراض ده مبني على شكل شاشة تحرير المنتج في السكرين المرفق
  مع الطلب ("All Brands" checkboxes + "+ Add New Brand" + رابط
  `edit-tags.php?taxonomy=product_brand`) — **لسه محتاج اختبار حقيقي بعد أول
  نشر** (زي ما اتأكد فلتر `global_unique_id` قبل كده 26-08-2026). لو الـ
  endpoint رجّع 404/401، يبقى المنصة مستخدمة تصنيف مخصّص من پلجن تاني (مش
  الـ Brands الأصلي)، ويحتاج تعديل `wcSearchBrands()`/تحديث المنتج ليستخدم
  endpoint مختلف (مثلاً `wp/v2/product_brand` عبر WP REST العام لو التاكسونومي
  `show_in_rest`).
- تأكيد ما إذا كانت WooCommerce REST secrets (`WC_BASE_URL` / `WC_CONSUMER_KEY` /
  `WC_CONSUMER_SECRET`) لازم تتضاف كأسرار — الكود بيستخدمها فعليًا (`wcGetProduct`
  وغيرها) رغم إن الوصف الأصلي قال "Shopify OAuth بس". لو الأداة رجّعت خطأ ناقص
  متغيرات WC عند أول تشغيل حقيقي، ضيفهم في الداشبورد وحدّث هذا الملف.
  (✅ عمليًا شغّالة فعليًا — الاختبار الحقيقي 26-08-2026 لـ `find_product`
  و`sync_product` نجح، يعني الأسرار دي متظبطة فعلًا في الداشبورد.)
- تسجيل `type` مخصّص لعمليات تحديث السعر في `ecommoda-constants` §7 (بدل
  إعادة استخدام `synced`/`error` مع `extra.priceApplied`) — اختياري، مش إلزامي
  دلوقتي. يستاهل لو محتاجين فلترة/تقرير منفصل لعمليات تحديث السعر في تاب السجل.
- **تغطية حالة "منتج لسه مش مربوط وعنوانه مختلف بين المنصتين"** — بعد ما اتشال
  fallback البحث بالرقم في الـ SKU (v2.5.0، لأنه أثبت إنه مش بيشتغل أصلًا)،
  البحث في `find_product` لمنتج لسه مش مربوط بقى معتمد بالكامل على تطابق
  العنوان (لتضييق النطاق) + GTIN/SKU (للتأكيد). لو العنوانين مختلفين بما يكفي
  إن `search=` الووردبريس ميلاقيش المنتج، النتيجة هتبقى "مفيش تطابق" حتى لو
  المنتج فعلًا موجود وSKU بتاعه يطابق الريجيكس. الحل الحالي يدوي (حط الرقم في
  GTIN من صفحة تعديل المنتج على ووردبريس بنفسك، والبحث هيلاقيه فورًا عن طريق
  فلتر GTIN). لو ده بيحصل بكثرة، يستاهل نبني حل تلقائي أشمل (مثلاً custom WP
  endpoint بيدوّر على الـ SKU مباشرة زي endpoint السعر بتاع أداة "مزامنة أسعار
  Stylebox").
